{
  "origin": "https://bloomflowers.example",
  "note": "The classic. A javascript: URL in a navigation action would execute in the page's context on tap \u2014 script execution driven by remote configuration, which ADR-003 forbids outright. Structurally valid JSON, so it MUST pass the schema \u2014 the scheme allow-list lives in the security layer.",
  "diagnostics": [
    {
      "code": "SS-E-SCHEME-DENIED",
      "disposition": "drop-item",
      "pointer": "/bottomNavigation/1/action/url"
    }
  ],
  "result": {
    "schemaVersion": "1.0",
    "origin": "https://bloomflowers.example",
    "site": {
      "id": "bloom-flowers",
      "name": "Bloom Flowers",
      "homeUrl": "https://bloomflowers.example/"
    },
    "bottomNavigation": [
      {
        "id": "home",
        "label": "Home",
        "icon": "home",
        "action": {
          "type": "internal_url",
          "url": "https://bloomflowers.example/"
        }
      }
    ]
  }
}
