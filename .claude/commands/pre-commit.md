# /pre-commit

Run `bash scripts/pre-commit-check.sh`.

Gitleaks, shellcheck, `:siteskin-core:test` with no Android SDK, `test`, `detekt`. All
unconditional — a check that skips itself silently is not a check.
