# /qa

Create `reports/qa/<TICKET>.md` from `reports/qa/template.md`. Only when checks are green.

Fill the scenario table with real results, and answer every templated edge case explicitly —
`N/A — no logic change.` is a valid answer, silence is not.
