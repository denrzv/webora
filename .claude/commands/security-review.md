# /security-review

Create `reports/security/<TICKET>.md` from `reports/security/template.md`.

Required for any ticket touching: manifest parsing or validation, origin handling, URL or intent
resolution, WebView configuration, or the browser's security chrome.
