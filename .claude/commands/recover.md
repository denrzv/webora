# /recover

Recover interrupted work: diff the tree against the tasklist, identify what actually landed, map the
remainder to a micro-task, run `/pre-commit`, commit.

Do not amend or rewrite existing commits — append.
