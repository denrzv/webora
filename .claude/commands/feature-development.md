# /feature-development

Full loop: `/idea` → `/researcher` → `/plan` → `/tasks` → implement tasks one at a time.

Before each commit: `/pre-commit`. Then `/review`, `/qa`, `/validate`. Squash-merge to `main` as
`<TICKET>: <title>`.
