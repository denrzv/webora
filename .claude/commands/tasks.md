# /tasks

Create `docs/tasklist/<TICKET>.md` with small tasks, each with acceptance criteria and named tests.

Tasks are `TASK-1..N`. Post-review findings are appended later as `TASK-FIX-N` with a `- Source:`
line recording provenance.

Set `Status: TASKLIST_READY` when done.
