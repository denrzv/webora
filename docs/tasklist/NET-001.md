# NET-001 tasklist

Status: TASKLIST_READY

## TASK-1 — Build the bounded origin-locked transport

- [x] Implement the app-layer OkHttp `ManifestSource` with fixed well-known path and explicit timeouts.
- [x] Reject non-HTTPS inputs, non-success responses, cross-origin redirects, a third redirect, oversized declared/streamed bodies, and I/O failures.
- [x] Add MockWebServer success and negative-control tests.
- [x] Run `bash scripts/pre-commit-check.sh`.

- Negative control: permitting a third redirect made `follows two exact origin redirects and refuses a third` fail because a fourth request reached the success response.

## TASK-2 — Discover and validate alongside page rendering

- [x] Add an injectable lifecycle coordinator that canonicalizes main-frame URLs, validates through `SiteSkinValidator`, cancels superseded discovery, and ignores stale results.
- [x] Forward page-start events from browser composition without awaiting discovery or applying SiteSkin chrome.
- [x] Add coroutine tests for HTTPS filtering, accepted/rejected validation, non-blocking dispatch, cancellation, and stale-result protection.
- [x] Run `bash scripts/pre-commit-check.sh`.

- Negative control: removing superseded-job cancellation made `returns immediately and cancels superseded discovery` emit both the stale and current outcomes.

## TASK-3 — Review, QA, validation, and closeout

- [x] Run review and resolve every finding through `TASK-FIX-*` commits if needed.
- [x] Produce a `QA_PASSED` report with focused and full-gate evidence.
- [x] Update shared architecture notes and mark `NET-001` complete in the roadmap.
- [x] Run `/validate` and `bash scripts/pre-commit-check.sh`.

## TASK-FIX-1 — Cancel superseded network calls and clean the test

- Source: `/review findings 1–2`
- [x] Replace blocking OkHttp execution with a cancellable await that cancels the underlying call.
- [x] Add a negative-control test proving coroutine cancellation reaches OkHttp and remove the duplicated assertion.
- [x] Run `bash scripts/pre-commit-check.sh`.
- Negative control: removing `invokeOnCancellation { call.cancel() }` made `cancelling fetch cancels the underlying call` hang/fail because the interceptor never observed cancellation.
