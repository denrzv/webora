# UX-012: Tasklist
Status: TASKLIST_READY

- [x] TASK-1 — Define and apply the mode-aware chrome projection
  - Add an exhaustive pure projection for Home, regular, and integrated visible layers.
  - Drive top, content-action, and bottom rendering from that projection without changing origin,
    consent, action-resolution, or system-Back authority.
  - Add the full journey, consent/no-activation, exact-origin exit, selected-tab isolation, manifest
    suppression, structural ownership, and UI-tag mutual-exclusion tests.
  - Update the shared architecture note and M8 roadmap status.
  - Tests: `./gradlew :app:testDebugUnitTest`, `./gradlew :app:compileDebugAndroidTestKotlin`,
    `./gradlew detekt`, `bash scripts/pre-commit-check.sh`.

- [x] TASK-2 — Complete review, QA, documentation, and validation
  - Review found no open architecture, security, test, or complexity defects.
  - QA covers the complete mode journey, consent outcomes, origin exit, selected-tab isolation,
    protected integrated top, and stale asset handling.
  - Runtime instrumentation and screenshots are explicitly unavailable without a device or `/dev/kvm`.
  - Tests: documentation/status checks and `bash scripts/pre-commit-check.sh`.
