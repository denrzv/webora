# UX-007: Tasklist
Status: TASKLIST_READY

References:
- PRD: `docs/prd/UX-007.prd.md`
- Research: `docs/research/UX-007.md`
- Plan: `docs/plan/UX-007.md`

## Tasks

- [x] TASK-1: Build the browser-owned adaptive consent action hierarchy
  - Modified: `BrowserAccessibility.kt`, `BrowserScreen.kt`, consent Compose tests.
  - Acceptance: one full-width vertical action block renders Allow as filled primary, Not now as
    outlined secondary, and Never as text tertiary; outside dismissal remains Not now. Compact-width
    and 200% tests pin order, readability, >=48 dp targets, full-width geometry, and all callbacks.
  - Tests: focused app tests/Android-test compilation and `bash scripts/pre-commit-check.sh`.
  - Result: implemented one full-width three-level stack and compiled its compact/200%-scale
    geometry and callback coverage. Runtime instrumentation and Pixel 6 capture are unavailable in
    this managed checkout because it has no connected Android device or `/dev/kvm`.

- [x] TASK-2: Record review, QA, and validation evidence
  - Modified/new: tasklist, `CLAUDE.md`, review and QA reports, roadmap when validated.
  - Acceptance: normative context records the fixed browser-owned hierarchy; review findings are
    resolved; QA records all runnable evidence and any Android runtime limitation; validation is
    complete.
  - Tests: documentation-only changes require the pre-commit gate, with no redundant focused suite.
  - Result: review resolved without corrective findings; QA passed all locally runnable gates and
    records the managed Android runtime limitation; architectural context and roadmap are updated.
