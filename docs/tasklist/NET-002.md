# NET-002 tasklist

Status: TASKLIST_READY

## TASK-1 — Add the exact-origin freshness cache

- [x] Add bounded in-memory entries keyed by canonical origin and trusted schema version, with an origin-scoped active-version index.
- [x] Parse cache freshness defensively and cap it at 24 hours using an injectable clock.
- [x] Add tests for origin/version isolation, replacement, defensive byte copies, malformed directives, and the TTL ceiling.
- [x] Restore the merged page-start forwarding path required by the pre-commit detekt gate.
- [x] Run `bash scripts/pre-commit-check.sh`.

## TASK-2 — Integrate conditional fetch and offline reuse

- [x] Expose closed fetched/not-modified/unavailable/rejected transport outcomes and send stored HTTP validators.
- [x] Orchestrate fresh hits, accepted writes, `304` refresh, exact-origin offline fallback, and fail-closed HTTP/validation outcomes in discovery.
- [x] Preserve cancellation, stale-result protection, redirect limits, bounded bodies, and non-blocking rendering.
- [x] Add focused tests and negative controls for conditional validators and cross-origin/non-offline fallback.
- [x] Run `bash scripts/pre-commit-check.sh`.

## TASK-3 — Review, QA, validation, and closeout

- [x] Run review and resolve every finding through `TASK-FIX-*` commits if needed.
- [x] Produce a `QA_PASSED` report with focused and full-gate evidence.
- [x] Update shared architecture notes and mark `NET-002` complete in the roadmap.
- [x] Run `/validate` and `bash scripts/pre-commit-check.sh`.
