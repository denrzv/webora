# UX-008: Tasklist
Status: TASKLIST_READY

References:
- PRD: `docs/prd/UX-008.prd.md`
- Research: `docs/research/UX-008.md`
- Plan: `docs/plan/UX-008.md`

## Tasks
- [x] TASK-1: Add browser-owned Back to integrated chrome
  - Modified: `app/src/main/java/app/webora/browser/siteskin/SiteSkinTopBar.kt`
  - Modified: `app/src/main/java/app/webora/browser/browser/BrowserScreen.kt`
  - Modified: `app/src/androidTest/java/app/webora/browser/siteskin/SiteSkinTopBarTest.kt`
  - New: `app/src/test/java/app/webora/browser/siteskin/SiteSkinNavigationContractTest.kt`
  - Acceptance: integrated top bar always exposes the local Back icon/label, enabled only by
    browser-observed history and dispatched through the existing controller; its 48 dp browser-token
    surface is independent of manifest styling; the identity row remains visible and site bottom
    navigation remains unchanged.
  - Tests: `./gradlew :app:testDebugUnitTest`; `./gradlew :app:compileDebugAndroidTestKotlin`;
    `bash scripts/pre-commit-check.sh`.
  - Negative control: make the Back surface consume `colors.background`; the source/runtime contract
    must fail, then restore browser-token styling.
  - Result: integrated mode now wires browser-observed history to a stable Back control before site
    branding; its local icon, browser copy, 48 dp target, disabled root state, and Webora-token
    container are independent of the unchanged SiteSkin bottom navigation and security identity.
  - Negative-control result: the contract's deliberately SiteSkin-coloured Back snippet is rejected;
    the production source using `MaterialTheme.colorScheme.surfaceContainer` passes.

- [x] TASK-2: Record UX-008 architecture, review, QA, and roadmap completion
  - Modified: `CLAUDE.md`, `docs/ROADMAP.md`
  - New: `reports/review/UX-008.md`, `reports/qa/UX-008.md`
  - Acceptance: architecture records the integrated navigation ownership boundary; review has no
    unresolved findings; QA records tests and Pixel 6 evidence or the managed-runtime limitation;
    roadmap marks UX-008 complete.
  - Tests: documentation-only task; `bash scripts/pre-commit-check.sh` remains the required commit
    gate.
  - Result: the ownership and placement decision is recorded; review is resolved with no fixes; QA
    passed all locally runnable checks and records the unavailable managed Android runtime; the
    roadmap marks UX-008 complete.
