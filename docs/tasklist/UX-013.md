# UX-013: Tasklist
Status: TASKLIST_READY

References:
- PRD: `docs/prd/UX-013.prd.md`
- Research: `docs/research/UX-013.md`
- Plan: `docs/plan/UX-013.md`

## Tasks

- [x] TASK-1: close the expressive presentation projection
  - New: `app/src/main/java/app/webora/browser/siteskin/ExpressiveSiteSkinPresentation.kt`
  - New: `app/src/test/java/app/webora/browser/siteskin/ExpressiveSiteSkinPresentationTest.kt`
  - Define an immutable internal projection containing only the selected existing
    `SiteSkinColorScheme` and a closed standard/reduced motion policy.
  - Its factory accepts a trusted `SiteSkinConfiguration` plus explicit browser-owned dark-theme
    and reduced-motion inputs; it delegates guarded colour derivation to `SiteSkinTheme`.
  - Acceptance: both theme selections are deterministic and retain guarded contrast; missing
    branding remains total; reduced motion selects only the closed immediate policy; reflection
    proves the model exposes no dimension, shape, URL, asset, action, navigation, callback,
    composable, arbitrary map, or animation value; `bash scripts/pre-commit-check.sh` passes.
  - Tests: `ExpressiveSiteSkinPresentationTest` (all cases), existing `SiteSkinThemeTest` unmodified.
  - Negative control: evaluate the reflection guard against a deliberately unsafe fixture class
    carrying a dimension/remote string and confirm it reports both forbidden capabilities before
    running the same guard green against production.
  - Result: four focused projection tests pass. The negative-control fixture was reported as
    `[remoteResource, rawCss]`, while the production model exposed no forbidden capability fields.
    `bash scripts/pre-commit-check.sh` passed.

- [x] TASK-2: build the curved header and floating dock primitives
  - New: `app/src/main/java/app/webora/browser/siteskin/ExpressiveSiteSkinChrome.kt`
  - New: `app/src/test/java/app/webora/browser/siteskin/ExpressiveSiteSkinChromeContractTest.kt`
  - New: `app/src/androidTest/java/app/webora/browser/siteskin/ExpressiveSiteSkinChromeTest.kt`
  - Add a deterministic browser-owned curved-bottom shape, curved branded header container, and
    floating branded dock. Geometry, minimum sizes, tonal/elevation treatment, and slot order are
    centralized compiled values; global safe-drawing insets stay with `BrowserScreen`.
  - Primitive parameters may accept the closed presentation and named composable slots, never raw
    configuration, arbitrary geometry/path/order/motion input, browser security data, or action
    callbacks derived from a site.
  - Acceptance: real `createOutline` checks show a bounded non-degenerate curve at 320 dp and larger
    bounds; source contracts reject configuration-driven geometry, browser-theme derivation,
    animation inputs, global inset reads, and conditional slot omission; Compose instrumentation
    compiles and covers 320 dp at 200% font scale in light/dark/reduced modes, complete tagged
    content, fixed slot order, and 48 dp dock targets; current production chrome has no new caller;
    `bash scripts/pre-commit-check.sh` passes.
  - Tests: `ExpressiveSiteSkinChromeContractTest` (all cases),
    `ExpressiveSiteSkinChromeTest.compactLargeTextKeepsRequiredContentAndTargets`,
    `ExpressiveSiteSkinChromeTest.themeAndMotionPoliciesKeepTheSameOwnedSlots`.
  - Negative controls: contract helpers must reject unsafe source snippets that read
    `SiteSkinConfiguration`, `WindowInsets.safeDrawing`, `MaterialTheme.colorScheme`, caller-provided
    dimensions/animation specs, or compose required slots conditionally. Temporarily flatten the
    production curve and confirm the outline assertion fails, then restore it.
  - Additional check: `./gradlew :app:compileDebugAndroidTestKotlin`; run connected instrumentation
    only if a device is present, without provisioning a software emulator.
  - Result: four JVM contract cases pass and the two-case instrumentation suite compiles. Compose's
    JVM `Path` is an Android stub whose `getBounds()` returns an empty rectangle, so the resolved
    test asserts the real `Outline.Generic` type plus the pure coordinates consumed by that path;
    the instrumented layout cases retain the actual Compose clip/layout check. A zero-depth
    negative control fails the curve predicate, and unsafe source reports all forbidden seams.
    No device is connected, so runtime instrumentation was not executed and no emulator was
    provisioned. `bash scripts/pre-commit-check.sh` passed.

- [x] TASK-3: record reviewed and QA-validated M9 ownership foundation
  - Modified: `CLAUDE.md`
  - Modified: `docs/ROADMAP.md`
  - New: `reports/review/UX-013.md`
  - New: `reports/qa/UX-013.md`
  - Run `/review` after TASK-2. Append and complete any findings as `TASK-FIX-*` commits before QA.
  - Run `/qa`, map every PRD criterion to evidence, and distinguish device execution from compiled
    instrumentation. Set `QA_PASSED` only if all required available gates are green and any missing
    runtime instrumentation is solely the documented managed-cloud device limitation.
  - Record in shared architecture guidance that `SiteSkinColorScheme` remains the complete
    website-controlled visual vocabulary; expressive geometry/motion/insets/order stay compiled;
    primitives are intentionally unwired; UX-014/UX-015 own browser identity/control integration.
  - Mark UX-013 complete in the roadmap and run `/validate` after the task commit.
  - Acceptance: review is `RESOLVED`; QA is `QA_PASSED`; CLAUDE/roadmap match the implementation;
    `bash scripts/pre-commit-check.sh` passes; `/validate` reports no workflow or repository drift.
  - Tests: documentation-only task, so no focused code test is required by the user instruction;
    the mandatory `bash scripts/pre-commit-check.sh` workflow gate still runs before commit.
  - Result: review is `RESOLVED` after `TASK-FIX-1`; QA is `QA_PASSED` with every PRD criterion
    mapped to evidence and unavailable runtime instrumentation identified as the no-device managed-
    cloud limitation. Shared architecture guidance records the closed projection, primitive-owned
    geometry/insets, unwired handoff, and reduced-motion obligation; the roadmap marks UX-013 done.
    `bash scripts/pre-commit-check.sh` passes.

## Review fixes

- [x] TASK-FIX-1: make the floating inset primitive-owned
  - Source: `/review` finding 1
  - Modified: `app/src/main/java/app/webora/browser/siteskin/ExpressiveSiteSkinChrome.kt`
  - Modified: `app/src/androidTest/java/app/webora/browser/siteskin/ExpressiveSiteSkinChromeTest.kt`
  - Wrap the painted/tagged dock row in a full-width outer container with a compiled horizontal
    inset and vertically center its content. Do not require UX-015 to provide the defining floating
    geometry through its caller modifier.
  - Acceptance: at 320 dp the tagged dock surface has positive left/right separation from its
    parent, all five 48 dp targets remain contained and centered, focused JVM/source contracts pass,
    instrumentation compiles, and `bash scripts/pre-commit-check.sh` passes.
  - Result: the painted dock is inset 12 dp inside a full-width browser-owned outer container and
    its row centers content vertically. The 320 dp instrumented fixture asserts both inset edges and
    all existing target checks remain. Focused JVM tests, Android-test compilation, and
    `bash scripts/pre-commit-check.sh` pass; runtime remains unavailable with no connected device.
