# UX-003: Tasklist
Status: TASKLIST_READY

- [x] TASK-1: Chrome contract and geometry
  - Modified: `app/src/main/java/app/webora/browser/design/WeboraDimensions.kt`,
    `app/src/test/java/app/webora/browser/browser/BrowserChromeContractTest.kt`
  - Acceptance: Direction A's address/dock geometry and opaque enabled-navigation colour contract are
    named without duplicating the 48 dp touch-target owner; pure/source negative controls prove an
    editable address cannot serve as the committed identity and prove browser navigation remains
    outside `BROWSER_CONTENT_TAG`.
  - Tests: `./gradlew :app:testDebugUnitTest detekt`
  - **Deviation:** `UX-002` had already named all four Direction A dimensions in `WeboraChrome`, so
    this task reused and pinned them rather than modifying `WeboraDimensions.kt`. The test also pins
    the absence of a second touch-target declaration and exercises both source guards against
    deliberately broken snippets.

- [x] TASK-2: Address pill, identity chip and navigation dock
  - New: `app/src/main/java/app/webora/browser/browser/BrowserChrome.kt`
  - Modified: `app/src/main/java/app/webora/browser/browser/BrowserScreen.kt`,
    `app/src/main/res/values/strings.xml`,
    `app/src/androidTest/java/app/webora/browser/browser/BrowserChromeTest.kt`
  - Acceptance: regular mode replaces the filled-button `FlowRow` with the 52 dp address pill,
    distinct tonal tagged identity chip and 60 dp floating icon dock; all existing commands and
    enabled states dispatch correctly; browser menu inventory is unchanged; the chip renders
    committed-origin presentation throughout address focus/edit and its negative control detects
    removal/rebinding; enabled unselected actions use opaque browser colour.
  - Tests: `./gradlew :app:testDebugUnitTest :app:compileDebugAndroidTestKotlin detekt`
  - **Deviation:** the selected sketch's reload/stop slot remains reload-only. Research confirmed
    `BrowserWebViewController` has no stop contract; presenting a stop glyph whose callback reloads
    would be a false affordance. No new strings were needed because every icon action already had a
    resource-backed accessible name.

- [x] TASK-3: Browser-owned error and persistent status presentation
  - Modified: `app/src/main/java/app/webora/browser/browser/BrowserChrome.kt`,
    `app/src/main/java/app/webora/browser/browser/BrowserScreen.kt`,
    `app/src/androidTest/java/app/webora/browser/browser/BrowserChromeTest.kt`
  - Acceptance: error presentation consumes browser tokens and bundled warning asset while retaining
    kind-specific copy, bounded domain, retry enabled rule and retry/home tags; the persistent status
    node remains polite on progress/completion and assertive on failure; recovery contracts remain
    green.
  - Tests: `./gradlew :app:testDebugUnitTest :app:compileDebugAndroidTestKotlin detekt`

- [x] TASK-4: Large-text proof and workflow documentation
  - Modified: `app/src/androidTest/java/app/webora/browser/browser/BrowserFontScaleTest.kt`,
    `docs/ROADMAP.md`, `CLAUDE.md`
  - Acceptance: a 200% font-scale instrumentation case asserts the regular chrome's security
    identity, address and menu/control semantics remain displayed without clipped load-bearing copy;
    the roadmap is checked and shared implementation context records the security/evidence decisions.
  - Tests: `./gradlew :app:testDebugUnitTest :app:compileDebugAndroidTestKotlin detekt`


- [x] TASK-FIX-1: Place the regular navigation dock below page content
  - Source: `/review finding 1`
  - Modified: `app/src/main/java/app/webora/browser/browser/BrowserChrome.kt`,
    `app/src/main/java/app/webora/browser/browser/BrowserScreen.kt`,
    `app/src/test/java/app/webora/browser/browser/BrowserChromeContractTest.kt`,
    `app/src/androidTest/java/app/webora/browser/browser/BrowserChromeTest.kt`
  - Acceptance: regular mode composes address/identity above `BROWSER_CONTENT_TAG` and the floating
    navigation dock below it; integrated bottom navigation remains unchanged; a negative control
    rejects both a dock nested in page content and a dock placed above the page.
  - Tests: `./gradlew :app:testDebugUnitTest :app:compileDebugAndroidTestKotlin detekt`
