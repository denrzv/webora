# UX-004: Tasklist
Status: TASKLIST_READY

## TASK-1 — Rebuild Home with Webora tokens
- [x] Replace the pre-refresh hierarchy with token-backed header, search surface, empty-state sections, and suggested-site cards.
- [x] Preserve address resolution and safe compiled suggestion navigation.
- [x] Add/extend focused tests for the home interaction contract.
- Tests: `./gradlew :app:testDebugUnitTest`, `./gradlew :app:compileDebugAndroidTestKotlin`, `bash scripts/pre-commit-check.sh`.

## TASK-2 — Rebuild onboarding progression
- [x] Apply Direction A tokens and clear progression/action hierarchy without changing the three-page flow.
- [x] Preserve Skip, Next, Start, saved page index, scrolling, and 200% font-scale usability.
- [x] Add focused Compose coverage for progression and completion.
- Tests: `./gradlew :app:testDebugUnitTest`, `./gradlew :app:compileDebugAndroidTestKotlin`, `bash scripts/pre-commit-check.sh`.

## TASK-3 — Rebuild privacy settings and record accessibility
- [x] Present the global SiteSkin toggle as one labelled list row with accessible name and state description.
- [x] Present each complete canonical origin in a wrapping row with a compact, origin-named reset action.
- [x] Preserve clear-data confirmation and close callbacks; group actions using Webora tokens.
- [x] Expand settings instrumentation tests and update `docs/accessibility/CONFORMANCE.md` for all changed surfaces.
- Tests: `./gradlew :app:testDebugUnitTest`, `./gradlew :app:compileDebugAndroidTestKotlin`, `bash scripts/pre-commit-check.sh`.

## Review / QA
- Review: `reports/review/UX-004.md` — `RESOLVED`, no fix tasks.
- QA: `reports/qa/UX-004.md` — `QA_PASSED`; runtime instrumentation and screenshots unavailable without `/dev/kvm` or a connected device.
