# CI-008: Tasklist
Status: TASKLIST_READY

References:
- PRD: `docs/prd/CI-008.prd.md`
- Research: `docs/research/CI-008.md`
- Plan: `docs/plan/CI-008.md`

## Tasks

- [x] TASK-1: Assert the hosted journey's Back prerequisite
  - Modified: `app/src/androidTest/java/app/webora/browser/visual/LiveSiteScreenshotTest.kt`.
  - Acceptance: after accepted integrated evidence and before the existing visible click, the tagged
    browser-owned Back control must be displayed and enabled; all transition and capture policy stays
    unchanged.
  - Tests: Android-test compilation, detekt, assertion-order check, negative control, and full
    pre-commit gate.
  - Result: the hosted journey now requires the browser-owned integrated Back affordance to be
    displayed and enabled immediately before its existing click. Android-test compilation, detekt,
    assertion-order and removal negative controls, and the full pre-commit gate pass.

- [x] TASK-2: Complete review, QA, documentation, and validation
  - Modified: `docs/BACKLOG.md`, `docs/ROADMAP.md`, `CLAUDE.md`, `docs/tasklist/CI-008.md`,
    `reports/review/CI-008.md`, `reports/qa/CI-008.md`.
  - Acceptance: review has no open findings; QA maps every PRD criterion honestly; documentation
    distinguishes local diagnostic hardening from pending hosted rollout; all artifact statuses and
    the pre-commit gate validate.
  - Tests: documentation/status checks and `bash scripts/pre-commit-check.sh`.
  - Result: review resolved with no findings; QA maps every acceptance criterion and records hosted
    runtime honestly as unavailable. Backlog, roadmap, and architecture context distinguish the
    earlier diagnostic from the pending two-run rollout gate; artifact status and pre-commit checks
    pass.
