# CI-010: Tasklist
Status: IMPLEMENTED_AWAITING_HOSTED_ACCEPTANCE

Issue: #115

## Accepted scope

CI-010 uses the only current deployed SiteSkin fixture: `https://denrzv.github.io/apps/bloom/`.
That fixture does not explicitly request `bouquet`, therefore the protocol default `auto -> drawer`
is the authoritative hosted presentation for this ticket. Explicit bouquet evidence is deferred until
a real deployed fixture requests it; Webora must not add a screenshot-only manifest override.

## Tasks

- [x] TASK-1: restore automatic post-merge Android visual acceptance
  - Keep `workflow_dispatch` for deliberate/cold reruns.
  - Add `push` on `main` with executable runtime/evidence path filters.
  - Docs/report-only changes do not match the filter and therefore do not boot the emulator.
  - Preserve one Pixel 6 / API 33 hosted emulator journey.
  - Serialize with `cancel-in-progress: false`; a later merge may wait but must not cancel evidence for
    an earlier merge.

- [x] TASK-2: make screenshot ownership accept only provably Webora-owned application subwindows
  - Source: Android screenshots run #41.
  - The drawer/Compose overlay may be focused as a generic `Pop-Up Window` even though Android reports
    it as `APPLICATION_SUB_PANEL` owned by `app.webora.browser.debug` and parented to MainActivity.
  - `ScreenEvidencePolicy` now accepts a generic focused title only when the same full `dumpsys window`
    proves all four properties: Webora remains `mFocusedApp`, the focused window block has the exact
    Webora package, the type is `APPLICATION_SUB_PANEL` (or API numeric 1002), and `mParentWindow`
    belongs to Webora.
  - Foreign parent/package, wrong type, unknown window and system/error-dialog cases remain fail-closed.
  - Host-side unit tests cover the positive and negative ownership fixtures.

- [x] TASK-3: pin hosted acceptance to the current default drawer contract
  - The existing live showcase and navigation smoke already require `SITESKIN_HUB_DRAWER_TAG` and the
    trusted Bloom action ids after UX-022.
  - The regular Google/example.com handoff continues to assert complete SiteSkin drawer/quick-action
    teardown.
  - The workflow summary names drawer as the expected default for the current deployed fixture.
  - No bouquet fixture, screenshot-only presentation override, DOM injection or JavaScript hook is
    introduced.

- [x] TASK-4: separate CI-009 historical status from current acceptance
  - CI-009 remains `QA_BLOCKED`: its required two consecutive cold green runs were never recorded in
    its tasklist/QA report; run #41 was a blocking ownership-guard failure.
  - Its M9 bouquet-era acceptance is now superseded by the UX-021/UX-022/BROWSE-011 runtime contract
    and CI-010. It must not be relabelled `QA_PASSED` retroactively.

- [ ] TASK-5: complete CI-010 hosted acceptance and close #115
  - Merge the accepted CI-010 implementation to `main`.
  - Obtain two consecutive cold post-merge/manual hosted runs of that accepted commit lineage.
  - Each run must report `test_status=0`, `png_count=6`, `tiles=6` and a green navigation smoke test.
  - Human review must inspect all six full-resolution frames plus `preview.png`.
  - Expected canonical visual story:
    1. `01-home.png`
    2. `02-siteskin-consent.png`
    3. `03-bloom-storefront.png`
    4. `04-bloom-actions.png` — default drawer open with trusted actions visible
    5. `05-bloom-profile.png`
    6. `06-google-regular.png` — regular browser chrome with no SiteSkin drawer/actions
  - Only after both runs are accepted: mark QA passed, close #115, and mark CI-009 historical acceptance
    superseded/closed without claiming it passed its obsolete two-run gate.

## Non-goals

- Hosted bouquet evidence without a real deployed explicit-bouquet fixture.
- A presentation matrix or multiple emulator boots.
- Pixel-perfect screenshot diffing.
- Making the hosted screenshot job a blocking PR required check.
