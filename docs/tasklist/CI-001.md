# CI-001: Tasklist
Status: TASKLIST_READY

References:
- PRD: `docs/prd/CI-001.prd.md`
- Research: `docs/research/CI-001.md`
- Plan: `docs/plan/CI-001.md`

## Tasks

- [x] TASK-1: Drive and capture the production live-site journey
  - New: `app/src/androidTest/java/app/webora/browser/visual/LiveSiteScreenshotTest.kt`
  - Acceptance: launch `MainActivity`; skip first-run onboarding; capture Home; open the compiled
    Bloom Flowers suggestion; require the exact canonical-origin consent title; capture consent;
    select Allow; require SiteSkin's browser-owned security indicator and bottom navigation; capture
    integrated mode. Each wait is bounded, each full-display bitmap and PNG write is asserted, and
    stable names land under app-specific external `files/screenshots`.
  - Tests: `./gradlew :app:assembleDebugAndroidTest`, `./gradlew :app:lintDebug`, and
    `bash scripts/pre-commit-check.sh`. Runtime is deferred to TASK-2's GitHub-hosted KVM job.
  - Result: instrumentation compilation and Android lint pass. The first compilation exposed an
    experimental Compose wait API and the replacement initially carried an invalid top-level import;
    both were corrected, and the final build/lint run is green. Runtime remains unavailable locally
    because this managed checkout has no `/dev/kvm`.

- [x] TASK-2: Run the screenshot journey manually on a GitHub-hosted emulator
  - New: `.github/workflows/android-screenshots.yml`, `docs/SCREENSHOTS.md`
  - Modified: `docs/ROADMAP.md`, `docs/DEVELOPMENT_PLAN.md`
  - Acceptance: `workflow_dispatch` only; least-privilege read permission; serialized, timed API 33
    emulator; immutable third-party action pin; focused instrumentation; deterministic `adb pull`;
    non-empty PNG guard; always-uploaded screenshots, test output and logcat with seven-day
    retention. Documentation tells the owner exactly how to run and download it and states the live
    GitHub Pages dependency.
  - Tests: parse the workflow as YAML, lint embedded shell with ShellCheck, compile instrumentation,
    run `./gradlew :app:lintDebug`, and run `bash scripts/pre-commit-check.sh`. Runtime screenshot
    production occurs after the commit is exported to GitHub because this container has no KVM.
  - Result: Ruby parsed the workflow YAML; all four extracted shell blocks pass ShellCheck; debug
    Android-test assembly and Android lint pass. `actionlint` is not installed in this environment.
    Runtime screenshot production awaits GitHub export because this managed checkout has no KVM.

- [x] TASK-3: Review, QA, validate, and close the ticket
  - New: `reports/review/CI-001.md`, `reports/qa/CI-001.md`
  - Modified: `docs/tasklist/CI-001.md`
  - Acceptance: review security and failure paths; record compile/static/full-gate evidence; run the
    workflow when GitHub execution is available and either link the resulting screenshot artifact or
    accurately record that runtime evidence awaits platform export; validate all workflow statuses
    and tasks.
  - Tests: `bash scripts/pre-commit-check.sh`.
  - Result: review found no changes required and records the debug/live-origin choices as deliberate;
    QA passes all locally available compilation, lint, YAML, ShellCheck, action-pin and full-gate
    scenarios. Runtime remains accurately recorded as environment-unavailable until platform export.
