# UX-009: Tasklist
Status: TASKLIST_READY

References:
- PRD: `docs/prd/UX-009.prd.md`
- Research: `docs/research/UX-009.md`
- Plan: `docs/plan/UX-009.md`

## Tasks

- [x] TASK-1: the container role stops being a control value
  - Modified: `app/src/test/java/app/webora/browser/design/WeboraThemeTest.kt`
  - Modified: `app/src/main/java/app/webora/browser/design/WeboraRadius.kt`
  - Modified: `app/src/main/java/app/webora/browser/browser/HomeScreen.kt`
  - The assertions are written **first and confirmed red against unmodified production code**, then
    the fix lands in the same commit — the repository's convention is that every commit is green and
    a control is run transiently, so "write the failing test" is a step inside this task rather than
    a checkpoint of its own.
  - Two assertions. One over the resolved geometry of every settable `Shapes` role laid out at
    `280 × 560` dp through the real `createOutline`, requiring each corner to be at most a quarter of
    the width; one structural, that no role carries `WeboraRadius.PILL`. The geometry one runs
    through Compose's own clamp rather than reading the token, because that is where a good number
    turns into a bad shape.
  - The 280 dp constant carries its provenance in a comment (material3's `internal` `DialogMinWidth`)
    and the argument for why transcribing it is safe: an absolute corner that clears the ceiling at
    the narrowest width clears it at every larger one.
  - Then `EXTRA_LARGE = 28.dp`, added to `ALL`; `WEBORA_SHAPES.extraLarge` reads it;
    `HomeScreen.kt:48` names `RoundedCornerShape(WeboraRadius.PILL)` directly so the address field
    stays a pill. KDoc records the token path that produced the circle.
  - Acceptance: both new assertions pass; `every shape corner comes from the compiled radii`,
    `every radius below the pill is a multiple of the same base` and `the shape scale covers every
    role the public API can set` pass **unmodified**; `UX-002`'s `C2` source scan and runtime sweep
    still pass; `bash scripts/pre-commit-check.sh` passes.
  - Tests: `WeboraThemeTest.a container role never rounds a dialog into a stadium`,
    `WeboraThemeTest.the pill is a control value and no shape role carries it`
  - Negative control: run both assertions against production code before the fix, and again with
    `extraLarge = RoundedCornerShape(WeboraRadius.PILL)` restored afterwards. Record what the failure
    message said the corner resolved to — a control that reports only "red" cannot distinguish the
    clamp from a compile error.
  - Result: 18 tests in `WeboraThemeTest` + `WeboraDimensionsTest`, 0 failures.
    `bash scripts/pre-commit-check.sh` OK.
  - **Control before the fix: 12 tests, both new assertions failed**, against production code that
    nothing else had touched. The message is the part worth keeping —
    `extraLarge resolves to a 140.0px corner on a 280.0px-wide dialog`, printed eight times (four
    corners × two axes). 140 is *exactly* half of 280, which is the clamp measured rather than
    argued: `CornerBasedShape` scaled a 999 dp request down to the largest radius the box admits,
    and the largest radius a box admits is the one that rounds its sides away entirely.
  - **Control after the fix (`extraLarge` restored to `PILL`): 18 tests, the same 2 failed**, and
    the other 16 — including `every shape corner comes from the compiled radii` — stayed green. That
    is the finding to keep: the provenance assertion is satisfied by the defect, because 999 dp is a
    declared token. Fitness and provenance are different questions and the old suite only asked one.
  - Both controls restored from a copy taken before editing, and the tests re-run green afterwards.

- [ ] TASK-2: the instrumented case, demoted honestly
  - Modified: `app/src/androidTest/java/app/webora/browser/browser/SiteSkinConsentDialogTest.kt`
  - One case rendering the consent dialog at 360 dp with a non-default-port origin, requiring the
    complete browser-authored heading to be displayed. Its comment states that it passes over a
    clipped heading and is therefore documentation of the `HARDEN-002` requirement, not the
    regression gate.
  - `androidTest` is not compiled by the pre-commit gate, so compile it deliberately:
    `./gradlew :app:compileDebugAndroidTestKotlin`.
  - Acceptance: the file compiles; the four existing cases are **unmodified**.
  - Tests: `SiteSkinConsentDialogTest.consentDialogRendersTheCompleteHeadingAtPhoneWidth`
  - Result:

- [ ] TASK-3: record the ticket
  - Modified: `CLAUDE.md`, `docs/BACKLOG.md`
  - A short note under the browser-owned design tokens section: the pill is a control value,
    `DialogTokens.ContainerShape` is `CornerExtraLarge`, `CornerBasedShape` scales an over-large
    corner instead of rejecting it, and `Surface` clips — the four facts that turn 999 dp into a
    clipped security heading. Backlog entry closed with the mechanism and the closing evidence.
  - Acceptance: `bash scripts/pre-commit-check.sh` passes.
  - Tests: the gate.
  - Result:
