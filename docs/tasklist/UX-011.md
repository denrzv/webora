# UX-011: Tasklist
Status: TASKLIST_READY

References:
- PRD: `docs/prd/UX-011.prd.md`
- Research: `docs/research/UX-011.md`
- Plan: `docs/plan/UX-011.md`

## Tasks

- [x] TASK-1: Evolve the dock into the shared browser shell
  - New: bundled Tabs vector.
  - Modified: dock API/layout, strings, focused Compose and source-contract tests.
  - Acceptance: the fixed browser-owned order is Back, Forward, Reload, Home, Tabs, More; Tabs opens
    the real callback directly; reload has explicit enabled state; every control has a bundled name,
    at least 48 dp target, and fits the compact test width; the dock accepts no manifest-controlled
    model, content, styling, ordering, or dispatch input.
  - Tests: focused Compose/source tests; `:app:testDebugUnitTest`;
    `:app:compileDebugAndroidTestKotlin`; `detekt`; pre-commit gate.
  - Result: the existing dock now exposes a direct bundled Tabs action in the fixed Back, Forward,
    Reload, Home, Tabs, More order and models reload availability explicitly. Compose tests cover
    callback dispatch, disabled semantics and 48 dp targets; a source negative control pins the
    manifest-free API and ordering, and the closed icon contract includes the browser-owned glyph.

- [x] TASK-2: Persist the shared shell across Home and regular browsing
  - Modified: `HomeScreen`, `BrowserScreen`, call sites and Home/font-scale instrumentation tests.
  - Acceptance: Home content scrolls above the same shared dock as regular pages; the separate Home
    Tabs button is removed; Home disables Back/Forward/Reload and keeps Home/Tabs/overflow active;
    regular actions use active-tab observed state/controller; safe-drawing insets remain consumed
    once and system Back retains the existing dispatcher contract; compact and 200%-font tests
    retain visible accessible controls.
  - Tests: Home/chrome/font-scale Compose sources compile; source negative controls; app unit suite;
    Android-test compilation; `detekt`; pre-commit gate.
  - Result: Home now scrolls above the same `BrowserNavigationShell` used after ordinary page
    content, replacing its one-off Tabs button. Its history/reload actions are explicitly disabled
    while Tabs and overflow use the common functional overlays; regular dispatch remains bound to
    the active controller. Contract and compiled Compose tests pin one safe-drawing inset owner,
    manifest isolation, Home enabled states, compact layout and 200%-font reachability.

- [x] TASK-3: Complete review, QA, documentation and validation
  - Modified: review/QA reports, `CLAUDE.md`, roadmap/backlog and task results.
  - Acceptance: review findings become provenance-linked `TASK-FIX-*` tasks/commits; runnable QA is
    green; unavailable device gesture/three-button runtime and screenshot evidence is explicit;
    normative docs pin browser/manifest/system ownership; validation reports no workflow drift.
  - Tests: documentation checks and final pre-commit gate.
  - Result: review found compact-width overflow and resolved it in `TASK-FIX-1`; QA passes all
    runnable scenarios and records unavailable device modes/screenshots explicitly. `CLAUDE.md`
    pins shell, manifest, active-tab and system-navigation ownership; roadmap/backlog and workflow
    artifacts are synchronized and validation is green.

- [x] TASK-FIX-1: Keep all six shell targets inside compact screens
  - Source: `/review finding 1`
  - Acceptance: a 320 dp host retains all six 48 dp browser actions without shell overflow or
    clipping; shared horizontal spacing stays token-owned.
  - Tests: compact-width Compose geometry negative control; app unit suite; Android-test
    compilation; `detekt`; pre-commit gate.
  - Result: the shared shell uses the 16 dp spacing token, leaving exactly 288 dp for six 48 dp
    targets on a 320 dp host. A compact-width Compose negative control compares every action's
    bounds to the shell bounds so restoring the 20 dp gutters fails the test.
