# UX-005: Tasklist
Status: TASKLIST_READY

References:
- PRD: `docs/prd/UX-005.prd.md`
- Research: `docs/research/UX-005.md`
- Plan: `docs/plan/UX-005.md`

## Tasks
- [x] TASK-1: Extend and publish the trusted semantic icon vocabulary
  - Modified: `siteskin-core/src/main/kotlin/dev/siteskin/core/SecurityValidator.kt`
  - Modified: `siteskin-core/src/test/kotlin/dev/siteskin/core/SecurityValidatorTest.kt`
  - Modified: `spec/SPEC.md`
  - Acceptance: catalog, flower, and search join the allow-list; Bloom Flowers retains its published compatible icon names;
    unknown and resource-like names normalize to generic with the existing diagnostic.
  - Tests: `./gradlew :siteskin-core:test`; `bash scripts/pre-commit-check.sh`.
  - Negative control: temporarily pass a resource-like name through normalization; the focused core
    assertion must fail, then restore the guard.
  - Result: the closed allow-list now accepts the published vocabulary while the pinned Bloom Flowers fixture remains byte-identical; `ic_launcher` normalizes to `generic` with `SS-W-ICON-UNKNOWN`.
  - Negative-control result: adding `ic_launcher` to the allow-list made the focused assertion fail;
    restoring the closed vocabulary returned the full core suite to green.

- [x] TASK-2: Replace integrated placeholder glyphs with bundled semantic vectors
  - New: SiteSkin vector resources under `app/src/main/res/drawable/`.
  - Modified: `app/src/main/java/app/webora/browser/siteskin/SiteSkinChrome.kt`
  - Modified: `app/src/test/java/app/webora/browser/design/BrowserIconContractTest.kt`
  - Modified: `app/src/androidTest/java/app/webora/browser/siteskin/SiteSkinChromeTest.kt`
  - Acceptance: all supported names use closed bundled-vector selection; unknown/null use generic;
    quick-action launcher previews a meaningful trusted icon; no old glyph remains; accessibility,
    selected semantics, minimum target, and large-text behavior remain intact.
  - Tests: `./gradlew :app:testDebugUnitTest`; `./gradlew :app:compileDebugAndroidTestKotlin`;
    `bash scripts/pre-commit-check.sh`.
  - Negative control: replace closed mapping with dynamic lookup; the source contract must fail.
  - Result: seven new vectors plus the existing home/menu/search assets cover the trusted vocabulary;
    the quick-action launcher previews its first action and a source contract forbids dynamic lookup
    and every prototype glyph.
  - Negative-control result: injecting `getIdentifier(` into the renderer made the source contract
    fail; restoring the closed mapping returned app unit tests to green.

- [x] TASK-3: Record UX-005 architecture, review, QA, and visual evidence
  - Modified: `CLAUDE.md`
  - New: `reports/review/UX-005.md`, `reports/qa/UX-005.md`
  - Acceptance: architecture records the semantic-token/resource boundary; review has no unresolved
    findings; QA records tests and Pixel 6 evidence or the managed-runtime limitation.
  - Tests: documentation-only task; `bash scripts/pre-commit-check.sh` remains the required commit gate.
  - Result: the architectural boundary is recorded; review is resolved and QA passed. Pixel 6
    capture is unavailable because `/dev/kvm` is absent and `adb devices` reports no device, while
    the instrumentation test sources compile successfully.
