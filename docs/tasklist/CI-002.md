# CI-002: Tasklist
Status: TASKLIST_READY

References:
- PRD: `docs/prd/CI-002.prd.md`
- Research: `docs/research/CI-002.md`
- Plan: `docs/plan/CI-002.md`

## Tasks

- [x] TASK-1: Build before boot, and refuse to run without the APKs
  - Modified: `.github/workflows/android-screenshots.yml`
  - Modified: `scripts/android-screenshot-ci.sh`
  - Acceptance: a `Build the app and Android-test APKs` step runs
    `./gradlew :app:assembleDebug :app:assembleDebugAndroidTest` **before** the emulator action, so
    the eight-and-a-half-minute build observed in run `31491580516` no longer overlaps emulator
    start-up. `scripts/android-screenshot-ci.sh` gains `require_prebuilt_apks`, which fails with both
    expected paths named when either APK is absent, before Gradle is invoked. Everything `CI-001`
    established is untouched: `workflow_dispatch`-only, `contents: read`, the pinned emulator-action
    SHA, the 40-minute timeout, the KVM and disk-space steps, the artifact name and globs, the PNG
    collection and the `png_count == 0` failure.
  - Tests: `shellcheck scripts/*.sh`; the workflow parses as YAML; `require_prebuilt_apks` exercised
    locally against a temporary tree with both APKs present, with one missing and with neither.
  - Negative control: none applicable — this task removes a cause and adds a precondition; the
    precondition's own failure path is exercised directly above.
  - Result: `shellcheck scripts/*.sh` clean; the workflow parses and its steps are ordered
    checkout → java → artifact → KVM → free disk → **build APKs** → emulator → upload → summary.
    `require_prebuilt_apks` exercised by sourcing the script (which now runs nothing on source) in a
    temporary tree: neither APK → exit 1 naming both paths; app APK only → exit 1 naming the test
    APK; both → exit 0. `./gradlew :app:assembleDebug :app:assembleDebugAndroidTest` then produced
    `app-debug.apk` (14,247,433 bytes) and `app-debug-androidTest.apk` (1,049,378 bytes) at exactly
    the two paths the script asserts, and the precondition passed against the real tree.
    `bash scripts/pre-commit-check.sh` OK.
  - Deviation: the build step is placed **after** `Free disk space for Android emulator`, not before
    it. That step deletes `$ANDROID_HOME/system-images`, `ndk` and `cmake` — none of which an
    assemble needs — so running the build afterwards keeps the freed space available for the AVD and
    keeps that step's `df` diagnostics meaningful.

- [x] TASK-2: A readiness gate that observes instead of sleeping
  - New: `scripts/android-emulator-ready.sh`
  - New: `scripts/android-emulator-ready-selftest.sh`
  - Modified: `scripts/android-screenshot-ci.sh`, `scripts/pre-commit-check.sh`
  - Acceptance: `readiness_verdict` is a pure shell function of four observed values — `boot_completed`,
    `bootanim_exit`, `pm_path`, `focus_line` — returning `ready` or a specific reason. `main` polls
    `adb`, requires `READY_SETTLE_SAMPLES` (default 3) consecutive `ready` verdicts, is bounded by
    `READY_DEADLINE_SECONDS` (default 180), and appends every sample with a timestamp to
    `artifacts/readiness.txt` whether it passes or fails. Sourcing the script executes nothing. It
    records the focused window's title and does **not** classify it — TASK-3 owns that knowledge, and
    a second copy in shell would be free to drift. `android-screenshot-ci.sh` runs the gate before
    Gradle and fails the run when it does not reach `ready`, with logcat and diagnostics still
    collected. `pre-commit-check.sh` runs the self-test unconditionally.
  - Tests: `android-emulator-ready-selftest.sh` — a fully ready sample is `ready`; each of the four
    conditions individually missing is not; a null `mCurrentFocus` is not; a device still showing the
    boot animation is not.
  - Negative control: remove one condition from `readiness_verdict` and confirm its case flips to
    `ready`; restore.
  - Result: 8 checks, all passing, wired into the gate — `==> emulator readiness self-test` now runs
    between shellcheck and the JVM suites. Negative control: deleted the `bootanim_exit` branch and
    the still-booting case flipped from `bootanim-running:0` to `ready`, 1 of 8 failing while the
    other seven kept passing — none of them stands in for that condition. Restored; 8 of 8.
    `bash scripts/pre-commit-check.sh` OK.
  - Deviation 1: the focus sample comes from `adb shell dumpsys window`, not the plan's
    `dumpsys window windows`. The bare form is the spelling that has been stable across API levels,
    and TASK-4 parses the same dump on the device side — two different commands would be two
    different contracts for one parser.
  - Deviation 2: an eighth check was added beyond the planned list — a System UI ANR dialog in
    `mCurrentFocus` is asserted to be `ready`. It pins the non-duplication decision the PRD makes in
    criterion 2: readiness reports the focused window and never classifies it. Without this
    assertion, a later well-meaning edit could add window classification here and nothing would
    object until the two copies disagreed.

- [x] TASK-3: One owner for what may be dismissed, and a JVM test that can fail
  - New: `app/src/screenshotPolicy/java/app/webora/browser/evidence/ScreenEvidencePolicy.kt`
  - New: `app/src/test/java/app/webora/browser/evidence/ScreenEvidencePolicyTest.kt`
  - Modified: `app/build.gradle.kts`
  - Acceptance: `focusVerdict(dumpsysWindowOutput, appPackage)` is pure Kotlin with no Android import
    and returns `OwnedByApp`, `DismissableSystemAnr(processName, title)` or `Blocked(reason)`. It
    parses every `mCurrentFocus=Window{… u0 <title>}` occurrence and requires exactly one distinct
    title. `DismissableSystemAnr` is reachable only for
    `Application Not Responding: com.android.systemui` — a System UI *crash* dialog, a Webora ANR, an
    unknown window, an absent or null focus and two disagreeing focuses are all `Blocked`. The
    allow-list is one constant containing one process name; there is no `else` branch that dismisses.
    `app/build.gradle.kts` shares `src/screenshotPolicy/java` into the `test` and `androidTest` source
    sets with both `java.srcDir` and `kotlin.srcDir`, and **not** into `main`;
    `:app:assertInspectorAbsentFromReleaseVariants` and `BrowserSurfaceConventionsTest` stay green.
  - Tests: `ScreenEvidencePolicyTest` — the six rows of the plan's decision table, plus `processName`
    extraction, plus a dump containing no `mCurrentFocus` at all.
  - Negative control: (1) add the app package to the allow-list — the Webora-ANR case must fail;
    (2) accept `Application Error:` as dismissable — the System-UI-crash case must fail; (3) drop the
    distinct-title requirement — the two-window case must fail. Restore after each.
  - Result: 14 tests, 0 skipped, 0 failures. All three negative controls behaved as designed and each
    failed *only* its own case, so none of the fourteen is standing in for another:
    (1) allow-listing `app.webora.browser.debug` → 1 failure, `a Webora ANR is never dismissed`;
    (2) treating `Application Error:` as dismissable → 2 failures, the Webora-crash and
    System-UI-crash cases; (3) dropping the distinct-title check → 1 failure, `two focused windows
    that disagree fail closed`. Restored after each; 14 of 14 green.
    `./gradlew :app:assembleDebugAndroidTest` compiles with the shared root on the `androidTest`
    classpath, and `bash scripts/pre-commit-check.sh` OK.
  - Deviation: ownership is a **whole-token** match on the package inside the window title, not the
    planned `<appPackage>/…` prefix. Two reasons, and both have a test. A dialog, popup or
    presentation window is not guaranteed to be titled `package/activity` — and the consent
    screenshot is taken with a dialog focused, so the strict shape risked failing the journey this
    guard protects. A bare `contains` was rejected in turn because it accepts
    `com.evil.app.webora.browser.debug` and `app.webora.browser.debugger` as Webora. The package must
    appear with a non-package character or nothing on each side; `a package that merely contains ours
    is not ours` and `a window Android did not title as package slash activity is still ours` pin
    both halves.

- [x] TASK-4: Refuse to photograph a screen Webora does not own
  - New: `app/src/androidTest/java/app/webora/browser/visual/ScreenEvidenceGuard.kt`
  - Modified: `app/src/androidTest/java/app/webora/browser/visual/LiveSiteScreenshotTest.kt`
  - Acceptance: `requireAppOwnsScreen(label)` reads `dumpsys window windows` through the
    `UiAutomation` the test already holds, classifies it with `ScreenEvidencePolicy`, and returns only
    on `OwnedByApp`. `Blocked` fails the test with the reason and writes the raw dump to
    `diagnostics/window-<label>.txt`. `DismissableSystemAnr` finds a clickable node by view id
    (`android:id/button2`, then `android:id/aerr_wait`), requires exactly one, records view id, node
    text and window title to `diagnostics/interference.txt`, clicks it and re-checks — at most two
    attempts, exhaustion fails. `FLAG_REPORT_VIEW_IDS` is set once on the service info, because
    without it the view-id lookup returns nothing and a recoverable run would fail as "no Wait
    button". `captureDeviceScreenshot` calls the guard after `waitForIdle()` and before
    `takeScreenshot()`; the journey, its matchers, the three file names and the storage path are
    unchanged. Diagnostics go through `PlatformTestStorage` into the tree the workflow already
    uploads, so no artifact path moves and `DEVX-002` starts from the layout it planned against.
  - Tests: `./gradlew :app:assembleDebugAndroidTest` compiles; `bash scripts/pre-commit-check.sh`
    passes. Execution is the hosted run — `AGENTS.md` forbids a software-only emulator here.
  - Negative control: not available on the JVM; the decision this task consumes carries its negative
    controls in TASK-3, which is why the classification was separated from the interaction.
  - Result: `./gradlew :app:assembleDebugAndroidTest` compiles the guard and its call site;
    `bash scripts/pre-commit-check.sh` OK. Not executed here — no `/dev/kvm`, per `AGENTS.md`. The
    hosted run in TASK-5 is where this task's behaviour is observed.
  - Deviation 1: a blocking verdict is **retried until a 20-second deadline** rather than failing on
    the first sample. Dismissing a dialog is asynchronous, and for a moment after `Wait` the focused
    window can legitimately be nothing at all; failing immediately would turn a recovered run red.
    The deadline still fails closed, carrying the last reason and the dump that produced it.
  - Deviation 2: interference is recorded as `diagnostics/interference-<label>-<attempt>.txt` rather
    than one appended file. `PlatformTestStorage.openOutputFile` truncates, and per-event files avoid
    depending on append semantics that vary by test-services version. A successful capture also
    writes `diagnostics/focus-<label>.txt` — the `mCurrentFocus` lines only — which is what makes the
    recorded fixtures in `ScreenEvidencePolicyTest` reconcilable against a real API 33 device. It is
    an excerpt, not a second classification.

- [x] TASK-5: Run the hosted workflow, then record what it showed
  - Modified: `docs/SCREENSHOTS.md`, `docs/ROADMAP.md`, `CLAUDE.md`, this tasklist
  - Acceptance: the `Android screenshots` workflow is dispatched on the ticket branch and its result
    recorded here — build-before-boot timings, whether the readiness gate settled and after how many
    samples, whether any interference was recorded, and whether the three canonical frames are clean.
    `docs/SCREENSHOTS.md` states what the pipeline may dismiss, what it will never dismiss, where
    `readiness.txt` and `diagnostics/` live, and what a red run now means. `ROADMAP.md` ticks
    `CI-002`. `CLAUDE.md` gains a `CI-002` note covering the evidence-integrity rule, the one-package
    allow-list and the shared `src/screenshotPolicy/java` root — the three things a later ticket
    might otherwise "simplify". No source, test or build file changes in this task.
  - Tests: `bash scripts/pre-commit-check.sh` passes.
  - Result: two hosted runs, and both are evidence.

    **[`31512048008`](https://github.com/denrzv/webora/actions/runs/31512048008) — red at the
    readiness gate.** `Boot completed in 89980 ms`, then 49 samples of `bootanim-running:<empty>`
    across the full 180-second deadline. Diagnosed from `readiness.txt` in one look and fixed as
    TASK-FIX-1. The pipeline failed instead of photographing a device it could not vouch for, which
    is the behaviour the ticket asked for even though the cause was the gate's own bug.

    **[`31513527146`](https://github.com/denrzv/webora/actions/runs/31513527146) — green, 11m24s.**

    | Evidence | Value |
    |---|---|
    | Build, outside the emulator step | `BUILD SUCCESSFUL in 5m 14s` |
    | Work left inside the emulator step | `75 actionable tasks: 1 executed, 74 up-to-date` — against 72 executed in `31491580516` |
    | Gradle inside the emulator step | 2m 22s, against 8m 39s before |
    | APK precondition | both APKs found, 14,247,433 and 1,060,644 bytes |
    | Readiness | `settled after 7 samples (33s)` |
    | Journey | `test_status=0`, `png_count=3`, 35 files uploaded against 28 |

    **The readiness samples are the finding worth keeping.** Sample 1 was `ready`; samples 2, 3 and 4
    were `no-focused-window:mCurrentFocus=null`; samples 5–7 were `ready` again. The device stopped
    owning its own display for about eleven seconds *after* `boot_completed`, and a gate that trusted
    one sample — or a short fixed `sleep` — would have started the journey inside that window. The
    settle requirement is load-bearing in practice, not only in principle.

    Three captures returned `OwnedByApp` against real API 33 `dumpsys window` output, which is the
    reconciliation `docs/research/CI-002.md` owed: the title format and the parser agree on a real
    device, and a parse failure would have failed the run rather than passing quietly. No System UI
    ANR appeared, so the dismissal path stayed unexercised in CI — the outcome the PRD's risk section
    predicted, and the reason its proof lives in TASK-3's negative controls.

- [x] TASK-FIX-1: Wait for the boot animation to not be running, not for a property this emulator
      never sets
  - Source: hosted run
    [`31512048008`](https://github.com/denrzv/webora/actions/runs/31512048008) — the first run of the
    new pipeline, red at the readiness gate
  - Modified: `scripts/android-emulator-ready.sh`, `scripts/android-emulator-ready-selftest.sh`
  - Acceptance: the second condition samples `init.svc.bootanim` and fails only while it reads
    `running`. `service.bootanim.exit == 1` was wrong for this workflow: the emulator is launched
    with `-no-boot-anim`, so the boot animation never runs and never sets that property. The gate
    therefore waited for a value the device could not produce — 49 samples, all
    `bootanim-running:<empty>`, its entire 180-second deadline, and a red job three minutes after
    `Boot completed in 89980 ms`. The condition being tested is "the boot animation does not own the
    display", which is false only while the service is running; `stopped` and unset both satisfy it.
  - Tests: two new self-test cases pin both spellings the emulator can present — unset under
    `-no-boot-anim`, and `stopped` — alongside the existing `running` case. The coverage floor rises
    from 8 to 10.
  - Negative control: restored the `!= "1"` comparison. 3 of 10 failed — both new cases and the
    ANR-classification case, all for the same reason. Restored; 10 of 10.
  - Result: `bash scripts/pre-commit-check.sh` OK. Worth recording plainly: **no shell self-test
    could have caught this.** It is a fact about the device, and the only instrument that can observe
    it is the hosted run — which is why the ticket runs one. What the gate did do is fail fast and
    name its own cause: the diagnosis took one look at `readiness.txt`, and a contaminated screenshot
    was never produced.

- [x] TASK-FIX-2: Run the readiness gate in CI, not only on a developer machine
  - Source: `/review` FINDING-1
  - Modified: `.github/workflows/ci.yml`
  - Acceptance: the `guardrails` job runs `scripts/android-emulator-ready-selftest.sh` as its own
    step. `scripts/pre-commit-check.sh` is not what CI runs — `guardrails` runs the pre-commit hooks,
    `android` runs Gradle tasks, `detekt` runs detekt — so a gate wired only into that script can be
    regressed by any pull request without CI noticing. The repository already mirrors gate items
    individually for this reason; `:app:assertInspectorAbsentFromReleaseVariants` has its own step.
  - Tests: the workflow parses and the `guardrails` job carries the step;
    `bash scripts/android-emulator-ready-selftest.sh` passes 10 of 10;
    `bash scripts/pre-commit-check.sh` OK.
  - Negative control: covered by TASK-2 and TASK-FIX-1 — the self-test this step runs is the one
    proven to fail when a readiness condition is removed.
