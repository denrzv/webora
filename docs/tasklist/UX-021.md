# UX-021: Tasklist

Status: TASKLIST_READY

References:
- Issue: [denrzv/webora#104](https://github.com/denrzv/webora/issues/104)
- PRD: `docs/prd/UX-021.prd.md`
- Research: `docs/research/UX-021.md`
- Plan: `docs/plan/UX-021.md`

The order is the plan's order and is not interchangeable. TASK-1 makes the signal honest; TASK-2
makes it emphatic. Shipping TASK-2 first would render a green trust mark over `origin.scheme`.

## Tasks

- [ ] TASK-1: Browser-owned transport state, derived from observed navigation
  - Modified: `app/src/main/java/app/webora/browser/browser/SecurityPresentation.kt`
  - Modified: `app/src/main/java/app/webora/browser/browser/BrowserState.kt`
  - Modified: `app/src/main/java/app/webora/browser/browser/RendererOwnership.kt`
  - Modified: `app/src/main/java/app/webora/browser/browser/BrowserScreen.kt`
  - Acceptance:
    - `TransportSecurity` has four values with `UNKNOWN` declared first.
    - `BrowserState.transport` defaults to `UNKNOWN`; `BrowserSessionSnapshot` is untouched.
    - `BrowserObservation.MainFrameCompleted` exists as its own case and `toBrowserObservation` no
      longer collapses `WebViewEvent.MainFrameCompleted` into `Page`. `WebViewEvent` gains no case.
    - Transitions: `PageStarted` → `UNKNOWN` unconditionally; `Page` → preserved only when the
      observed `SiteOrigin` equals the committed one, else `UNKNOWN`; `MainFrameCompleted` →
      `completedTransport(origin, failure)`; `PageFailed(TLS)` → `TLS_ERROR`, every other kind →
      `UNKNOWN`, never preserved.
    - `completedTransport` returns `SECURE` only for `https` with no recorded failure; `http` is
      `NOT_SECURE`; a null origin, a non-HTTP(S) scheme and a non-null failure are all `UNKNOWN`.
    - `navigateFromHome` resets to `UNKNOWN`; `activateSiteSkin`/`deactivateSiteSkin` preserve.
    - `securityPresentation(mode, transport)` still returns `null` for `Home` and for an origin-less
      `Regular`; `BrowserScreen:698`'s `checkNotNull` still holds.
    - No manifest field, page content, editable address text or `WebView.getUrl()` appears in any
      signature on the path.
  - Tests:
    - `BrowserStateTest` — the nine rows in the plan's table (issue reqs. 1–5).
    - `SecurityPresentationTest` — four states carried through, null contract preserved,
      `completedTransport` table including the non-HTTP(S) row.
    - `RendererOwnershipTest` — issue req. 6: a background tab's transport transition updates that
      tab only.
  - Negative controls (record each result here when run):
    - `completedTransport` returns `SECURE` whenever `origin != null` → must fail the HTTP, failure
      and non-HTTP(S) rows.
    - `PageStarted` preserves instead of resetting → must fail the loading case and the
      TLS-then-navigate case.
    - `MainFrameCompleted` mapped back onto `BrowserObservation.Page` → must fail the
      completed-vs-changed distinction.
    - `PageFailed` preserves transport → must fail the no-green-survives-TLS case.
    - Router re-pointed at `session.activeId` → must fail the new transport case **while the file's
      existing discriminator still passes** (`BROWSE-009`'s rule: a control that fails everything
      proves the tests run, not that they test the rule).

- [ ] TASK-2: Regular chrome reads the four states
  - Modified: `app/src/main/java/app/webora/browser/browser/BrowserChrome.kt`
  - Modified: `app/src/main/res/values/strings.xml`
  - Acceptance:
    - `BrowserSecurityIdentity` selects its label from an exhaustive `when` with no `else`, so a
      fifth state is a compile error.
    - `security_unknown` and `security_tls_error` added; `ic_lock` for `SECURE`, `ic_warning`
      otherwise; ground and roles unchanged.
    - Issue req. 12: both chrome variants read one `SecurityPresentation` and cannot classify one
      page differently — there is still exactly one classification site.
  - Tests:
    - `BrowserChromeTest` — the existing committed-origin-vs-edited-address case still passes
      (`UX-003`'s deliberate disagreement), extended for an unknown-transport load.
    - `BrowserChromeContractTest` — `BROWSER_SECURITY_TAG` still refused on the address field.

- [ ] TASK-3: Two shield drawables and the icon budget raise
  - New: `app/src/main/res/drawable/ic_shield_secure.xml`
  - New: `app/src/main/res/drawable/ic_shield_neutral.xml`
  - Modified: `app/src/test/java/app/webora/browser/design/BrowserIconContractTest.kt`
  - Acceptance:
    - Stroke-only, 1.9 weight, one declared colour, no `android:fillColor`, the shared viewport.
    - Distinct internal marks, so the two states differ without colour (issue req., WCAG 1.4.1).
    - `BUDGET` 18 → 20 as a deliberate raise recorded in the test's comment, with `UX-005` as
      precedent; `EXPECTED` gains both names.
  - Tests:
    - `BrowserIconContractTest` — the existing viewport/stroke/colour/fill/geometry assertions cover
      the new drawables with no new test.

- [ ] TASK-4: The compact inline trust chip replaces the full-width row
  - Modified: `app/src/main/java/app/webora/browser/siteskin/SiteSkinTopBar.kt`
  - Modified: `app/src/main/res/values/strings.xml`
  - New: `app/src/test/java/app/webora/browser/siteskin/SiteSkinTopBarContractTest.kt`
  - Acceptance:
    - The full-width `surfaceContainer` `SecurityIdentity` row is gone, with it the `Secure` word and
      the `·` separator (issue req. 1).
    - `SiteSkinSecurityChip` is a **fixed sibling declared after the `weight(1f)` title `Column`**
      inside the title `Row`, so a long title cannot displace it (issue req. 10, PRD risk 6).
    - Ground is `MaterialTheme.colorScheme.primaryContainer` / `onPrimaryContainer` — which map to
      `container` / `onContainer`, the only ground `secure`/`notSecure` are measured on. **Not**
      `surfaceContainer`, which maps to `chrome` and is unmeasured for these roles.
    - The chip shows the browser-derived registrable domain, one line, ellipsized — `ADR-006` stays
      intact and unamended (plan decision 1).
    - Tint is `secure` for `SECURE` and `notSecure` for the other three; glyph is the matching
      shield; the `when` is exhaustive with no `else`.
    - `SITESKIN_SECURITY_TAG` keeps its exact value on the chip's node with a browser-authored
      `contentDescription` naming transport state and domain (issue reqs. 9, 13).
    - `siteskin_tls_secure`, `siteskin_tls_not_secure`, `siteskin_security_separator` deleted.
    - No `presentation.colors` or `SiteSkinColorScheme` member reaches the chip.
  - Tests:
    - `SiteSkinTopBarContractTest` (new) — source scan over **executable lines only** (`BROWSE-009`:
      strip `*`, `//`, `/*` first): chip names `primaryContainer`, names no SiteSkin colour, tints
      from `secure`/`notSecure`, applies `testTag(SITESKIN_SECURITY_TAG)` as an *application* rather
      than a declaration (`UX-020`), and the deleted row and separator are absent.
    - `SiteSkinTopBarModelTest` — unchanged model contract still holds.
  - Negative controls (record each result here when run):
    - Chip ground changed to `presentation.colors.background` → must fail the contract scan.
    - Chip tinted from the SiteSkin scheme → must fail the contract scan.
    - Chip declared before the title `Column` → must fail the 320 dp bounds assertion in TASK-5.

- [ ] TASK-5: Instrumented contract and the five `SITESKIN_SECURITY_TAG` call sites
  - Modified: `app/src/androidTest/java/app/webora/browser/siteskin/SiteSkinTopBarTest.kt`
  - Acceptance:
    - Issue req. 7 lands on the existing 320 dp bounds assertion: a title long enough to ellipsize
      leaves the chip's node inside the header bounds.
    - Issue req. 8: a hostile manifest title and colours cannot alter the chip's tagged description.
    - All five existing call sites re-checked rather than assumed — `LiveSiteScreenshotTest:124,
      133-136`, `LiveSiteNavigationSmokeTest:123,173-176,204`, `SiteSkinTopBarTest:39,57,90-92`.
      `CI-009` is pending hosted acceptance on exactly these, so the node must stay displayed in
      integrated mode and absent otherwise.
    - `./gradlew :app:compileDebugAndroidTestKotlin` passes — the gate does not compile
      `androidTest`, so this is asserted explicitly and not inferred from a green `test`.
  - Tests:
    - `SiteSkinTopBarTest` — long-title bounds, hostile-manifest description.
    - Instrumented suites are **evidence, never a gate claim** (`A11Y-001`); hosted acceptance for
      the frames remains `CI-009`'s.

- [ ] TASK-6: Documentation
  - Modified: `CLAUDE.md`
  - Modified: `docs/DEVELOPMENT_PLAN.md`
  - Acceptance:
    - A `UX-021` section records: that transport state is browser-observed and written only through
      `routeRendererEvent`; that `MainFrameCompleted` is the *successful*-completion signal and
      `PageChanged` is not; that `SECURE` is the only value requiring positive evidence; that the
      chip grounds on `container` because that is where `secure`/`notSecure` are measured, and
      `surfaceContainer` maps to `chrome`; and that `ADR-006` was **kept, not amended**, with the
      reason the issue's literal layout was not taken.
    - No ADR is amended. `ADR-006` is cited, unchanged.
  - Tests: none — documentation.

## Definition of done

Every acceptance criterion in issue #104 is met except the deliberate divergence recorded in plan
decision 1 (the registrable domain stays visible inside the chip rather than leaving integrated
chrome), `bash scripts/pre-commit-check.sh` is green, `:app:compileDebugAndroidTestKotlin` compiles,
and every negative control above has a recorded result.
