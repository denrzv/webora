# NET-003 tasklist

Status: TASKLIST_READY

References:
- PRD: `docs/prd/NET-003.prd.md`
- Research: `docs/research/NET-003.md`
- Plan: `docs/plan/NET-003.md`

## TASK-1 — Add exact-origin bounded asset transport and pure policy

- [x] Define the closed transport result, PNG/WebP format recognition, browser-owned transfer and
  dimension limits, and deterministic monogram policy.
- [x] Implement HTTPS-only exact-origin fetching with at most two exact-origin redirects, strict
  media types, explicit timeouts, sentinel reading, and socket cancellation.
- [x] Add policy and MockWebServer tests for allowed fetches, cross-origin/subdomain/port redirects,
  redirect limits, SVG/mislabeled types, byte bounds, timeout, and cancellation.
- [x] Run negative controls by removing origin equality and the byte sentinel; the exact-origin
  redirect test and oversized-stream test each failed as required.
- [x] Run `bash scripts/pre-commit-check.sh`.

## TASK-2 — Add bounded decode, fallback orchestration, and lifecycle

- [x] Add the injectable decoder contract and thin off-main `BitmapFactory` bounds/full decoder.
- [x] Add a loader that verifies signature/type agreement and dimensions before full decode and
  returns a closed bitmap/monogram result for every non-cancellation path.
- [x] Add a one-job coordinator that cancels superseded work and prevents cancelled/stale results
  from publishing.
- [x] Add JVM tests for probe-before-decode, format/dimension rejection, fallback, exception and
  cancellation behavior; add real-decoder instrumentation coverage and compile it.
- [x] Run negative controls by bypassing the dimension check and stale-publication guard; the
  loader policy test and superseded-load coordinator test each failed as required.
- [x] Run `bash scripts/pre-commit-check.sh`.

## TASK-3 — Review, QA, validation, and closeout

- [x] Run review and resolve every finding through `TASK-FIX-*` commits if needed; review found no
  issues requiring a fix task.
- [x] Produce a `QA_PASSED` report with focused, instrumentation-compilation, and full-gate evidence.
- [x] Update shared architecture notes and mark `NET-003` complete in the roadmap.
- [x] Run `/validate` and `bash scripts/pre-commit-check.sh`; all artifact statuses and tasks are
  complete and the local gate passes. Branch CI is unavailable because this managed checkout has no
  configured remote; instrumentation runtime is unavailable because no device or `/dev/kvm` exists.
