# CI-007: Tasklist
Status: TASKLIST_READY

References:
- PRD: `docs/prd/CI-007.prd.md`
- Research: `docs/research/CI-007.md`
- Plan: `docs/plan/CI-007.md`

## Tasks
- [x] TASK-1: Add the fourth canonical regular-browsing frame
  - Modified: `app/src/androidTest/java/app/webora/browser/visual/LiveSiteScreenshotTest.kt`,
    `evidence-sheet/src/test/kotlin/app/webora/evidence/ContactSheetTest.kt`,
    `evidence-sheet/src/test/kotlin/app/webora/evidence/MainTest.kt`.
  - Acceptance: the live journey proves integrated chrome first, leaves through browser-owned UI,
    submits the reserved example destination, requires the regular identity/navigation layer and
    absence of every SiteSkin layer, and captures `04-regular-browsing.png` through the unchanged
    ownership/rendered guard; contact-sheet tests pin all four frames and `tiles=4`.
  - Tests: `./gradlew :evidence-sheet:test`, `./gradlew :app:testDebugUnitTest`,
    `./gradlew :app:compileDebugAndroidTestKotlin`, `./gradlew detekt`, negative controls, and
    `bash scripts/pre-commit-check.sh`.
  - Result: the journey uses the fixed SiteSkin Back affordance and Webora address input, then
    requires the regular identity/shell and absence of integrated identity, bottom navigation, and
    quick actions before the unchanged rendered capture. Four-frame composer/command tests pass.
    Replacing frame 04's expected marker with frame 03's makes the ordering test fail; restoring the
    assertion returns it green. Android instrumentation compiles; runtime capture is unavailable in
    this managed-cloud checkout without a connected device or `/dev/kvm`.

- [x] TASK-2: Complete review, QA, documentation, and validation
  - Modified: `docs/SCREENSHOTS.md`, `CLAUDE.md`, `docs/ROADMAP.md`,
    `docs/tasklist/CI-007.md`, `reports/review/CI-007.md`, `reports/qa/CI-007.md`.
  - Acceptance: review findings are resolved or appended as `TASK-FIX-*`; QA maps every PRD criterion
    to executable or explicitly environment-limited evidence; documentation describes four canonical
    frames without claiming an unrun hosted result; validation confirms workflow/status consistency.
  - Tests: documentation/status checks and `bash scripts/pre-commit-check.sh`; hosted cold runs are
    recorded as pending when no push-capable remote/device is available.
  - Result: review finding 1 was resolved in `TASK-FIX-1`; QA passes all runnable checks and
    maps every acceptance criterion. Four-frame documentation, roadmap, and architecture context are
    current. Hosted cold runs and runtime screenshot inspection remain explicitly pending because
    this checkout has no remote, connected device, or `/dev/kvm`.

- [x] TASK-FIX-1: Derive the regular security assertion from browser resources
  - Source: `/review finding 1`
  - Modified: `app/src/androidTest/java/app/webora/browser/visual/LiveSiteScreenshotTest.kt`,
    `reports/review/CI-007.md`, `docs/tasklist/CI-007.md`.
  - Acceptance: the expected regular identity uses the same browser-owned localised transport string
    as production and retains the fixed observed example domain.
  - Tests: `./gradlew :app:compileDebugAndroidTestKotlin`, `./gradlew detekt`, and
    `bash scripts/pre-commit-check.sh`.
  - Result: the assertion now resolves both the transport label and complete identity through the
    same browser-owned resources as production; instrumentation compilation, detekt, and the full
    pre-commit gate pass.
