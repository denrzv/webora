# UX-018 tasklist — Privacy settings modal

Status: TASKLIST_READY

References:
- PRD: `docs/prd/UX-018.prd.md`
- Plan: `docs/plan/UX-018.md`

## Tasks

- [x] TASK-1: isolate and dismiss privacy settings as a real modal window
  - Add a full-window opaque dialog host around the existing scrollable privacy content.
  - Route Close and Android Back through the same dismissal callback before browser navigation.
  - Keep SiteSkin/site content outside the presentation boundary and retain clear-data confirmation.
  - Acceptance: PRD criteria 1–8.
  - Tests: `./gradlew :app:testDebugUnitTest :app:compileDebugAndroidTestKotlin`, focused connected
    `PrivacySettingsScreenTest`, manual `denrzv.github.io` Close/Back journey, and
    `bash scripts/pre-commit-check.sh`.
  - Result: production now hosts the existing scrollable privacy content in a full-size opaque
    Compose `Dialog`; Close and platform Back share one dismissal callback and page input cannot
    fall through. Focused connected tests passed 6/6, the live Bloom journey passed Close/Back and
    nested-confirmation checks, and the full pre-commit gate is green.
