# NET-004: Tasklist
Status: TASKLIST_READY

References:
- PRD: `docs/prd/NET-004.prd.md`
- Plan: `docs/plan/NET-004.md`

## Tasks

- [x] TASK-1: Typed refusals in the brand-asset transport
  - Modified: `app/src/main/java/app/webora/browser/siteskin/OkHttpBrandAssetSource.kt`
  - Modified: `app/src/test/java/app/webora/browser/siteskin/OkHttpBrandAssetSourceTest.kt`
  - Acceptance: `BrandAssetFetchResult.Rejected` carries a closed `BrandAssetRejection`, an optional
    HTTP status and a redirect count; `Fetched` carries status and redirects. Every existing
    condition is unchanged — only the value returned on failure differs. `Unavailable` stays a bare
    object, because no answer has no status to report.
  - Tests: one assertion per reason (`NOT_HTTPS`, `MALFORMED_URL`, `CROSS_ORIGIN`, `HTTP_ERROR`,
    `REDIRECT_LIMIT`, `CROSS_ORIGIN_REDIRECT`, `UNSUPPORTED_MEDIA_TYPE`, `OVERSIZED`).
  - Negative control: **run.** Collapsing `OVERSIZED` into `HTTP_ERROR` in `readResponse` fails
    `rejects declared and streamed oversized bodies plus HTTP failure and timeout` at
    `OkHttpBrandAssetSourceTest.kt:178`; restoring it returns the suite to green. The existing
    `NET-003` assertions passed in both states — this task changed the vocabulary, not the policy.
  - Result: nine reasons, each asserted; a redirect with no `Location` is `MALFORMED_URL` rather than
    being folded into the origin check, and `Unavailable` stays a bare object.

- [ ] TASK-2: The loader names the stage that produced the monogram
  - New: `BrandAssetStage`, `BrandAssetTrace`, `BrandAssetOutcome`
  - Modified: `app/src/main/java/app/webora/browser/siteskin/BrandAssetLoader.kt`
  - Modified: `app/src/test/java/app/webora/browser/siteskin/BrandAssetLoaderTest.kt`
  - Acceptance: `load` returns `BrandAssetOutcome(asset, trace)`. Each refusal path names its own
    stage; `DECODED` carries the decoded dimensions; every load carries elapsed milliseconds.
    `CancellationException` still propagates rather than becoming a stage.
  - Tests: seven refusals produce seven distinct stages, and the published `BrandAsset` for each is
    exactly what it is today.
  - Negative control: the pre-change code cannot satisfy the stage assertions at all — every case
    returns the same `Monogram("B")`. Record that the new assertions fail against the old loader,
    which is the whole reason this ticket exists.

- [ ] TASK-3: The trace carries it, and the inspector says it in one line
  - Modified: `SiteSkinTrace.kt`, `SiteSkinTraceRecorder.kt`, `InspectorState.kt`,
    `InspectorSnapshot.kt`, `SiteSkinInspectorPanel.kt` (debug), `strings.xml`, `BrowserScreen.kt`
  - New: `app/src/main/java/app/webora/browser/siteskin/BrandAssetPublication.kt`
  - Deleted: `BrandAssetCoordinator.kt` and `BrandAssetCoordinatorTest.kt`
  - Acceptance: the recorder holds brand-asset traces in a second bounded map under the same
    canonical-origin key, evicted by the same helper, cleared by the same `clear()`.
    `inspectorRecorder()` becomes process-scoped in the debug variant, matching its own KDoc.
    The panel shows the stage, and the rejection when there is one, as browser-authored labels beside
    their values. `publishesBrandAsset` holds `NET-003`'s superseded-work guard as a pure function.
    Release variants gain nothing.
  - Tests: recorder bounds/eviction/clear; snapshot carries stage and rejection, absence renders as
    absent; `publishesBrandAsset` publishes the current configuration and refuses a superseded one;
    `SiteSkinTraceNeutralityTest` extended to the asset pipeline.
  - Negative control: drop the `===` guard and the superseded case must fail; record the recording /
    discarding sink comparison so the neutrality claim is not decoration.
  - Gate: `:app:assertInspectorAbsentFromReleaseVariants` still passes.

- [ ] TASK-4: The device records what the pipeline decided
  - Modified: `app/src/androidTest/.../visual/LiveSiteScreenshotTest.kt`
  - Acceptance: between Allow and frame 03 the journey waits, bounded, for the brand-asset trace to
    exist; writes `brand-asset-03-siteskin-integrated.txt` into the diagnostics directory with stage,
    rejection, status, redirects, dimensions and elapsed time — on success as well as failure, and
    `NEVER DECIDED` plus the budget on timeout; captures the frame; and only then asserts `DECODED`.
    Nothing is suppressed, pre-seeded or staged, and no frame that fails today can start passing.
  - Tests: `./gradlew :app:assembleDebugAndroidTest` — the gate never compiles this source set.
  - Note: no workflow change. The journey class is the one `SCREENSHOT_TEST_CLASS` already selects.

- [ ] TASK-5: Take the measurement
  - Acceptance: one `workflow_dispatch` run of `Android screenshots` on this branch. Record the run
    number, run id and commit SHA, the recorded stage/reason/elapsed, and which of the backlog's
    candidates the evidence convicts — including if it exonerates the pipeline entirely.
  - Output: `reports/qa/NET-004.md` gains the measurement; the finding drives TASK-6.
  - Rule: read the artifact whose SHA matches the commit being judged. A stale artifact looks exactly
    like a current one.

- [ ] TASK-6: Fix what the evidence convicts
  - Acceptance: a hosted `03-siteskin-integrated.png` shows the manifest's logo in the 40 dp slot,
    with the registrable domain and TLS state still present. `NET-003`'s caps, allow-list,
    same-origin recheck and monogram fallback are unchanged, proven by its tests passing unedited.
  - Scope is decided by TASK-5 and not before. If the evidence says the refusal is correct, this task
    becomes the record of why, with the inspector line as the deliverable.

- [ ] TASK-7: Record the ticket
  - Modified: `CLAUDE.md`, `docs/BACKLOG.md`
  - Acceptance: the architecture note says what the reason vocabulary is for, why the recorder is
    process-scoped, and what the harness may and may not do about a missing logo. The backlog entry
    closes with its evidence cited by run and SHA.
