# UX-017 tasklist — Branded integrated dock and coordinated lower edge

Status: TASKLIST_READY

## Tasks

- [x] TASK-1: finish the protected integrated dock identity and layout contract
  - Render the five fixed equal slots as one guarded SiteSkin-colored pill without neutral tile masks.
  - Add a dedicated stable circular Brand Hub control that consumes only the already guarded current
    `BrandAsset`, retains localized action semantics, and falls back deterministically.
  - Keep Back/Forward/Tabs/More local, ordered, typed, and browser-owned; keep the existing closed
    SiteSkin semantic icon vocabulary and deterministic generic fallback.
  - Prove 320 dp/200% targets and geometry, bounded asset/fallback presentation, command closure,
    content-before-dock reservation, persistent-nav absence, and regular-mode teardown.
  - Tests: `./gradlew :app:testDebugUnitTest :app:compileDebugAndroidTestKotlin` and focused connected
    Compose tests when a runtime exists.
  - Acceptance: PRD criteria 1–11 and 13.
  - Result: the guarded secondary pill is now continuous; four fixed local browser commands render
    directly over it and the center slot is a stable 52 dp primary circle showing the current
    configuration-guarded bitmap or deterministic monogram/flower fallback. The existing sibling
    WebView/dock layout is pinned as content reservation, persistent SiteSkin navigation remains
    absent, and tests close command order, remote asset reach, Bloom semantic icons, fallback,
    compact large-text targets, and regular teardown.

- [x] TASK-2: complete review, QA, visual evidence, and durable documentation
  - Run `/review`; append each actionable finding as a sourced `TASK-FIX-N` before QA.
  - Run `/qa`, focused/full gates, and the canonical Bloom plus regular screenshots on a connected
    runtime; otherwise record the managed-cloud runtime limitation.
  - Record the enduring integrated dock ownership/asset/reservation contract in shared architecture
    guidance and complete `/validate`.
  - Tests: documentation-only unless review/QA changes code; `bash scripts/pre-commit-check.sh` is
    still required before commit.
  - Acceptance: PRD criteria 12–13 with all criteria traced in QA.
  - Result: review resolved with no fix tasks and QA traces all criteria to automated or compiled
    evidence. The canonical runtime screenshots are explicitly limited by no connected device and
    no `/dev/kvm`; no prohibited software-only emulator was provisioned. Shared architecture now
    records the sole asset-bearing command and measured sibling reservation contract.
