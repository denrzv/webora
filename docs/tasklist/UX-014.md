# UX-014: Tasklist
Status: TASKLIST_READY

References:
- PRD: `docs/prd/UX-014.prd.md`
- Research: `docs/research/UX-014.md`
- Plan: `docs/plan/UX-014.md`

## Tasks

- [x] TASK-1: build the adaptive expressive integrated header
  - Modified: `app/src/main/java/app/webora/browser/siteskin/SiteSkinTopBar.kt`
  - Modified: `app/src/main/java/app/webora/browser/browser/BrowserScreen.kt`
  - Modified: `app/src/androidTest/java/app/webora/browser/siteskin/SiteSkinTopBarTest.kt`
  - Modified: `app/src/test/java/app/webora/browser/siteskin/SiteSkinNavigationContractTest.kt`
  - Refactor the top bar to accept the closed expressive presentation and render through
    `ExpressiveSiteSkinHeader`; keep Back first and browser-token styled, site branding bounded, and
    canonical domain/TLS in a separately tagged Webora-token sub-surface.
  - Use content-driven browser-owned adaptive layout so 320 dp/200% text can grow without hiding,
    overlapping, or clipping Back, branding, or identity. Preserve the current public-in-module
    semantics tags and localized identity description.
  - Acceptance: existing identity/logo/Back behavior stays green; header and identity bounds remain
    displayed and non-overlapping at 320 dp/200% in light/dark/reduced fixtures; Back retains a 48 dp
    target and disabled-root behavior; source contracts reject SiteSkin influence over Back,
    identity, geometry, slot presence/order, and motion; `bash scripts/pre-commit-check.sh` passes.
  - Tests: `SiteSkinTopBarTest` (all), `SiteSkinNavigationContractTest` (all), existing
    `ExpressiveSiteSkinChromeContractTest` and `ExpressiveSiteSkinPresentationTest`.
  - Negative control: feed the source-contract helper an unsafe fixture that uses SiteSkin colours
    for identity/Back and conditionally omits identity; confirm it rejects the fixture before
    checking production green.
  - Deviation: migrate the production call site with a temporary standard-motion input in this task
    so the refactored API remains buildable; TASK-2 replaces it with the platform motion preference.
  - Additional check: `./gradlew :app:compileDebugAndroidTestKotlin`; run connected instrumentation
    only if a device is present, without provisioning a software emulator.
  - Result: integrated top chrome now fills the curved UX-013 primitive with a fixed Back/brand row
    followed by an unconditional Webora-token canonical identity surface. Focused source negative
    controls reject conditional SiteSkin-coloured identity, JVM contracts pass, and the compact
    200% font instrumentation case compiles. No connected device is available for runtime execution.

- [x] TASK-2: wire active-tab theme and reduced-motion presentation
  - New: `app/src/main/java/app/webora/browser/design/MotionPreference.kt`
  - New: `app/src/test/java/app/webora/browser/design/MotionPreferenceTest.kt`
  - Modified: `app/src/main/java/app/webora/browser/browser/BrowserScreen.kt`
  - Add a thin Android animator-scale reader plus pure conservative mapping, then construct
    `ExpressiveSiteSkinPresentation` only in `PROTECTED_INTEGRATED` from the active trusted
    configuration, system theme, and browser motion choice.
  - Retain exact-origin/active-tab handoff, configuration-identity asset guard, Back callback,
    status/error placement, and current tabs/overflow/bottom reachability byte-for-byte in behavior.
  - Acceptance: zero/unavailable/invalid scale selects reduced behavior and positive scale selects
    standard; only protected integrated top composition constructs/passes expressive presentation;
    regular/Home chrome and SiteSkin bottom/menu composition are unchanged; focused handoff, state,
    asset, motion, and source contracts pass; `bash scripts/pre-commit-check.sh` passes.
  - Tests: `MotionPreferenceTest`, `ChromeHandoffTest`, relevant `BrowserStateTest` and brand-asset
    publication tests, plus focused top source contracts.
  - Negative control: prove the pure mapping helper rejects a deliberately permissive fixture that
    treats missing/invalid animator state as standard motion.
  - Additional check: `./gradlew :app:compileDebugAndroidTestKotlin`; run connected instrumentation
    only if a device is present, without provisioning a software emulator.
  - Result: the protected-integrated composition now constructs the closed presentation from its
    active trusted configuration, system theme, and a thin browser-owned animator-scale reader.
    The pure policy fails closed for zero, invalid, or unavailable scale; its permissive negative
    fixture demonstrates the missing-state gap. Focused motion, handoff, state, and stale-asset
    publication tests pass, and instrumentation compiles without a connected runtime device.

- [x] TASK-3: record review, QA, architecture, and roadmap completion
  - New: `reports/review/UX-014.md`
  - New: `reports/qa/UX-014.md`
  - Modified: `CLAUDE.md`
  - Modified: `docs/ROADMAP.md`
  - Run `/review` after TASK-2. Append and complete any findings as `TASK-FIX-*` commits before QA.
  - Run `/qa` and map every PRD criterion to test/source evidence. Compile instrumentation and run
    it/capture the visible change only when a connected device exists; otherwise record the managed-
    cloud no-device limitation without provisioning a software emulator.
  - Record the new curved header ownership contract, browser-token identity surface, animator-policy
    wrapper, active-tab/origin handoff, and that UX-015—not this ticket—moves tabs/overflow/dock.
  - Mark UX-014 complete in the roadmap and run `/validate` after the task commit.
  - Acceptance: review is `RESOLVED`; QA is `QA_PASSED`; shared guidance and roadmap match shipped
    code; `bash scripts/pre-commit-check.sh` passes; `/validate` reports no workflow/repository drift.
  - Tests: documentation-only task, so no focused code test is required by the user instruction;
    the mandatory `bash scripts/pre-commit-check.sh` workflow gate still runs before commit.
  - Result: review is `RESOLVED` after `TASK-FIX-1`; QA is `QA_PASSED` with every PRD criterion
    mapped to green automated or compiled evidence. Shared guidance records the expressive identity,
    active-tab/origin, motion, and UX-015 handoff contracts, and the roadmap marks UX-014 complete.
    Runtime instrumentation/screenshots remain unavailable because no device or `/dev/kvm` exists.

## Review fixes

- [x] TASK-FIX-1: cover every compact theme and motion pairing
  - Source: `/review finding 1`
  - Modified: `app/src/androidTest/java/app/webora/browser/siteskin/SiteSkinTopBarTest.kt`
  - Modified: `reports/review/UX-014.md`
  - Iterate the 320 dp/200% fixture across light/standard, dark/standard, and dark/reduced inputs;
    assert required header, Back, brand, and identity nodes, the 48 dp Back target, and non-overlap.
  - Acceptance: the same ownership/layout assertions run for every PRD-required theme/motion
    pairing; instrumentation compiles; focused JVM contracts and `bash scripts/pre-commit-check.sh`
    pass; review becomes `RESOLVED`.
  - Result: the compact 200% fixture now runs the identical required-slot, 48 dp Back, and ordered
    bounds checks for light/standard, dark/standard, and dark/reduced. Instrumentation compiles, the
    focused source contracts pass, and review is `RESOLVED` with no open findings.
