# UX-016 tasklist — Even regular dock command slots

Status: TASKLIST_READY

## Tasks

- [x] TASK-1: distribute the regular browser commands into equal slots
  - Make the dock row fill its surface and center each fixed command in one equal-width slot.
  - Preserve command order, callbacks, enabled states, 48 dp targets, shell padding, insets, and the
    separate protected integrated presentation.
  - Add Compose geometry assertions for equal slot widths/centers, symmetric edges, compact width,
    and existing interaction behavior.
  - Tests: `./gradlew :app:testDebugUnitTest :app:compileDebugAndroidTestKotlin`
  - Acceptance: PRD criteria 1–7 and 9–10.
  - Result: the pill's inner row now fills its width and centers the unchanged six commands in equal
    weighted, tagged slots. Compact-width Compose geometry evidence pins equal widths and centers,
    full edge coverage, symmetric outer spacing, and the existing 48 dp/interaction contracts.

- [x] TASK-2: complete review, QA, visual evidence, and durable documentation
  - Run `/review`; convert any actionable finding into a `TASK-FIX-N` before QA.
  - Run `/qa`, including focused/full gates and a Home screenshot when an Android runtime is
    available; otherwise document the managed-cloud limitation.
  - Record the stable regular-dock ownership/layout contract in shared architecture guidance and
    complete `/validate`.
  - Tests: documentation-only unless review/QA changes code; `bash scripts/pre-commit-check.sh` is
    still required before commit.
  - Acceptance: PRD criteria 8 and 10, with all earlier criteria traced in QA.
  - Result: review is resolved with no fix tasks; QA traces every criterion to green automated or
    compiled evidence. Home screenshot/runtime inspection is explicitly limited by the absence of a
    connected device and `/dev/kvm`; no prohibited software-only emulator was provisioned.
