# UX-015: Tasklist
Status: TASKLIST_READY

References:
- PRD: `docs/prd/UX-015.prd.md`
- Research: `docs/research/UX-015.md`
- Plan: `docs/plan/UX-015.md`

## Tasks

- [x] TASK-1: build the fixed expressive dock and separated navigation hub
  - New: `app/src/main/java/app/webora/browser/siteskin/SiteSkinDock.kt`
  - Modified: `app/src/main/java/app/webora/browser/siteskin/SiteSkinChrome.kt`
  - Modified: `app/src/main/res/values/strings.xml`
  - New: `app/src/androidTest/java/app/webora/browser/siteskin/SiteSkinDockTest.kt`
  - Modified: `app/src/test/java/app/webora/browser/siteskin/SiteSkinNavigationContractTest.kt`
  - Fill `ExpressiveSiteSkinDock` with five explicit equal browser-owned command slots in fixed
    Back/Forward/brand hub/Tabs/More order. Use local icons/strings, observed enabled inputs,
    explicit callbacks, browser-token command treatment, and minimum 48 dp targets.
  - Refactor the existing SiteSkin menu into a navigation hub with distinct navigation, quick
    action, and browser sections. Preserve bounded labels, local decorative icon mapping, selected
    route semantics, typed `NavigationItem` callbacks, favourite, and closed browser commands.
  - Acceptance: compact 320 dp/200% light/dark/reduced fixtures retain five ordered displayed 48 dp
    targets and isolated callbacks; hub headings and selected semantics separate site/browser
    content; unsafe source fixtures using generic lists or SiteSkin-controlled browser order/style
    are rejected; instrumentation compiles; `bash scripts/pre-commit-check.sh` passes.
  - Tests: `SiteSkinDockTest`, `SiteSkinNavigationContractTest`, `SiteSkinChromeModelTest`, existing
    `ExpressiveSiteSkinChromeContractTest` and `ExpressiveSiteSkinChromeTest`.
  - Negative control: run dock ownership helper against a fixture whose manifest-derived list
    controls command order/callbacks and verify rejection before production.
  - Additional check: `./gradlew :app:compileDebugAndroidTestKotlin`; execute instrumentation only
    with a connected device and do not provision a software-only emulator.
  - Result: the reusable dock now exposes five fixed, Webora-token command targets in the required
    order, and the hub projects bounded navigation, quick actions, site menu, and a separately
    headed browser section through typed callbacks. Source negative controls reject generic
    manifest command lists; focused JVM tests pass and instrumentation compiles without a device.

- [x] TASK-2: integrate dock and hub at the active protected projection
  - Modified: `app/src/main/java/app/webora/browser/browser/BrowserScreen.kt`
  - Modified: `app/src/androidTest/java/app/webora/browser/browser/BrowserSiteSkinLayoutTest.kt`
  - Modified: `app/src/test/java/app/webora/browser/siteskin/SiteSkinNavigationContractTest.kt`
  - Compute one active trusted chrome model/presentation, remove the integrated persistent bottom
    navigation and over-content quick-action launcher, and render the fixed dock. Route the brand
    button and `open_menu` to the same hub; reuse current typed site resolver, tab switcher, and
    browser menu state/callbacks.
  - Close/invalidate the hub on page start, active session/tab change, SiteSkin deactivation, and
    loss of protected integrated mode. Store visibility only; never store a controller, tab id,
    configuration, raw URL, or selected site action.
  - Acceptance: protected integrated composition contains the expressive dock/hub and not the old
    bottom/overlay surfaces; Back/Forward/Tabs/More invoke current browser callbacks; active route
    and typed site action dispatch remain correct; regular/Home and tab/origin handoff contain no
    stale brand/hub; focused state/handoff/model tests and pre-commit gate pass.
  - Tests: `BrowserSiteSkinLayoutTest`, `SiteSkinNavigationContractTest`, `ChromeHandoffTest`,
    `BrowserSessionTest`, `BrowserStateTest`, `SiteSkinChromeModelTest`, action resolver/nav matcher.
  - Negative control: source/composition fixture retaining old bottom navigation or a remembered
    selected item/controller must fail the ownership/invalidation helper before production green.
  - Additional check: `./gradlew :app:compileDebugAndroidTestKotlin`; run connected instrumentation
    and capture the perceptible change only if a device exists, otherwise record the limitation.
  - Result: protected integrated mode now replaces the persistent site navigation/quick-action
    layers with the five-command expressive dock. Brand and More open the same current-model hub;
    page starts, configuration/tab changes, and non-integrated projection invalidate it. Focused
    handoff/session/source tests pass and instrumentation compiles without a connected device.

- [x] TASK-3: record review, QA, architecture, and roadmap completion
  - New: `reports/review/UX-015.md`
  - New: `reports/qa/UX-015.md`
  - Modified: `CLAUDE.md`
  - Modified: `docs/ROADMAP.md`
  - Run `/review` after TASK-2. Append and complete every finding as a `TASK-FIX-*` commit before QA.
  - Run `/qa`, map every PRD criterion to automated/source/runtime evidence, compile instrumentation,
    and execute/capture only with a connected device; otherwise record the managed-cloud limitation.
  - Record fixed dock ownership/order, separated typed hub, active-tab/origin invalidation, and
    unchanged regular shell. Mark UX-015 complete and run `/validate` after the task commit.
  - Acceptance: review is `RESOLVED`; QA is `QA_PASSED`; shared guidance/roadmap match production;
    `bash scripts/pre-commit-check.sh` passes; `/validate` reports no workflow/repository drift.
  - Tests: documentation-only task, so no focused test is needed under the user's instruction; the
    mandatory pre-commit workflow gate still runs before commit.
  - Result: review is `RESOLVED` after `TASK-FIX-1`; QA maps every criterion to green source, JVM,
    or compiled instrumentation evidence. Shared guidance records dock/hub ownership and teardown,
    and the roadmap marks UX-015 complete. Runtime instrumentation/screenshots remain unavailable
    because this managed-cloud checkout has no connected device or `/dev/kvm`.

## Review fixes

- [x] TASK-FIX-1: scope selected semantics to site navigation
  - Source: `/review finding 1`
  - Modified: `app/src/main/java/app/webora/browser/siteskin/SiteSkinChrome.kt`
  - New: `app/src/androidTest/java/app/webora/browser/siteskin/SiteSkinNavigationHubTest.kt`
  - Modified: `reports/review/UX-015.md`
  - Apply selected/not-selected state descriptions only to navigation rows, never quick actions,
    favourite, or browser commands. Render the hub in instrumentation and prove separate headings,
    selected route state, absent false action state, and typed callback dispatch.
  - Acceptance: hub semantics truthfully distinguish selectable route state from actions; focused
    instrumentation compiles; JVM contracts and `bash scripts/pre-commit-check.sh` pass; review is
    `RESOLVED`.
  - Result: selection is now nullable and emitted only for navigation rows; quick actions,
    favourite, and browser commands expose no false route state. Hub instrumentation covers all
    three headings, selected semantics, absent action state, and original typed item dispatch.
