# CI-006: Tasklist
Status: TASKLIST_READY

References:
- PRD: `docs/prd/CI-006.prd.md`
- Plan: `docs/plan/CI-006.md`

## Tasks
- [x] TASK-1: Require repeated device-quiet CPU intervals before screenshot instrumentation
  - Modified: `scripts/android-emulator-ready.sh`, `scripts/android-emulator-ready-selftest.sh`
  - Acceptance: CPU counters are parsed fail-closed; baseline, busy, reset, and invalid intervals do
    not count toward settling; each sample records raw and derived CPU state; all existing boot,
    PackageManager, focus, deadline, and capture contracts remain unchanged.
  - Tests: `bash scripts/android-emulator-ready-selftest.sh`, `bash -n scripts/android-emulator-ready.sh scripts/android-emulator-ready-selftest.sh`, negative controls, `bash scripts/pre-commit-check.sh`.
  - Result: 23 readiness checks pass, including exact threshold, rounded-above-threshold, first
    baseline, malformed input, counter reset, and zero-delta cases. Removing busy rejection fails the
    rounded-above-threshold case; accepting a baseline fails its dedicated case; accepting every
    non-busy result fails both baseline and missing-observation cases. The implementation was restored
    byte-for-byte after each control. `bash scripts/pre-commit-check.sh` passes.

- [ ] TASK-2: Record hosted evidence and the durable readiness decision
  - Modified: `CLAUDE.md`, `docs/ROADMAP.md`, `docs/tasklist/CI-006.md`
  - Acceptance: two consecutive hosted runs each produce three canonical frames without a contested
    capture; readiness artifacts show the new condition sample by sample; documentation records the
    result without claiming that a quiet run exercised an ANR path.
  - Tests: `bash scripts/pre-commit-check.sh` (documentation-only task; no additional code test).
