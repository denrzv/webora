# CI-009: Tasklist
Status: TASKLIST_READY

References:
- PRD: `docs/prd/CI-009.prd.md`
- Research: `docs/research/CI-009.md`
- Plan: `docs/plan/CI-009.md`

## Tasks

- [x] TASK-1: expand and pin the guarded M9 visual evidence inventory
  - Modified: `app/src/androidTest/java/app/webora/browser/visual/LiveSiteScreenshotTest.kt`
  - Modified: `app/src/test/java/app/webora/browser/evidence/ExpressiveBloomJourneyContractTest.kt`
  - Modified: `evidence-sheet/src/test/kotlin/app/webora/evidence/MainTest.kt`
  - Modified: `evidence-sheet/src/test/kotlin/app/webora/evidence/ContactSheetTest.kt`
  - Modified: `docs/SCREENSHOTS.md`, `docs/WALKTHROUGH.md`
  - Capture eight ordered states through the unchanged ownership/rendered guard; require expressive
    browser chrome around storefront, product, history, and hub states; require the browser-authored
    native hub boundary; retain complete regular-mode teardown.
  - Acceptance: pure tests pin all eight filenames/order/captions and fail on an unsafe/partial story;
    every WebView frame requires page pixels; collection/composition production remains dynamic;
    focused tests, instrumentation compilation, lint, and pre-commit pass.
  - Tests: `./gradlew :app:testDebugUnitTest --tests '*ExpressiveBloomJourneyContractTest'`,
    `./gradlew :evidence-sheet:test`, `./gradlew :app:compileDebugAndroidTestKotlin`,
    `./gradlew detekt`, mutation negative control, and pre-commit.
  - Runtime: run/capture only with a connected device or KVM-hosted workflow; never provision a
    managed-cloud software emulator.
  - Result: the black-box journey now records eight guarded M9 states with browser identity/dock,
    history-control, native-hub, and exact-origin teardown prerequisites. JVM negative controls pin
    the complete chronological inventory and contact-sheet captions/tiles; instrumentation compiles,
    focused tests and detekt pass. Runtime capture is unavailable locally because `/dev/kvm` is
    absent and `adb devices` reports no connected target.

- [x] TASK-2: record review, local QA, architecture, and hosted acceptance blocker
  - New: `reports/review/CI-009.md`, `reports/qa/CI-009.md`
  - Modified: `CLAUDE.md`
  - Run `/review`; append and complete any `TASK-FIX-*` findings before `/qa`. Map every PRD criterion
    to automated and runtime evidence, record unavailable hosted evidence without weakening it, and
    preserve the remaining rollout as an explicit task.
  - Acceptance: review `RESOLVED`, local evidence green, honest QA status recorded, shared
    architecture updated, and the hosted blocker is not misreported as acceptance.
  - Tests: documentation/report changes need no focused test under the user's instruction; the
    mandatory pre-commit gate still runs before commit.
  - Result: review is `RESOLVED` after TASK-FIX-1 and all local automated/compile/lint gates pass.
    QA remains blocked on successful hosted evidence; run #33 supplied a real runtime failure that is
    addressed by TASK-FIX-2 rather than being misreported as acceptance.

- [ ] TASK-3: complete two-run hosted visual acceptance and close M9
  - New/modified: `reports/qa/CI-009.md`, `docs/ROADMAP.md`, `docs/tasklist/CI-009.md`
  - Dispatch the final commit twice from cold GitHub-hosted Pixel 6 runners, inspect all eight
    full-resolution frames and the eight-tile preview, record immutable run/artifact verdicts, set QA
    to `QA_PASSED`, mark CI-009/M9 complete, run `/validate`, and close issue #88.
  - Acceptance: two consecutive complete uncontested runs, human visual verdict accepted, roadmap
    checked, validation green, and issue closed.
  - Current blocker: hosted run #33 stopped after frame 03 because the journey looked for clickable
    text `Happy Days Bouquet`, while the deployed storefront exposes the clickable title `Happy Days`
    below the first viewport. TASK-FIX-2 aligns the selector and scroll behavior; two fresh cold runs
    on the fixed commit are still required.

## Review fixes

- [x] TASK-FIX-1: wait for black-box page transitions before product/history captures
  - Source: `/review finding 1`
  - Modified: `app/src/androidTest/java/app/webora/browser/visual/LiveSiteScreenshotTest.kt`
  - Modified: `app/src/test/java/app/webora/browser/evidence/ExpressiveBloomJourneyContractTest.kt`
  - Modified: `reports/review/CI-009.md`
  - Use the accessible clickable Happy Days link as a readiness sentinel: require it absent on the
    product/Forward states and present on the restored storefront before guarded capture. It remains
    an interaction/readiness observation, never a trust predicate.
  - Acceptance: no product/history capture can run while the prior page's link state remains;
    browser-owned tags and the unchanged guard still authorize every capture; focused JVM test,
    instrumentation compilation, detekt, and pre-commit pass.
  - Result: the clickable Happy Days link must disappear before product/Forward evidence and return
    before storefront-Back evidence. Its presence is only black-box readiness; integrated tags and
    `ScreenEvidenceGuard` remain the trust/capture authority. The negative contract and all focused
    compile/lint checks pass.

- [x] TASK-FIX-2: align the Happy Days black-box interaction with the deployed storefront
  - Source: hosted `Android screenshots` run #33 (`31929274541`).
  - Modified: `app/src/androidTest/java/app/webora/browser/visual/LiveSiteScreenshotTest.kt`.
  - Modified: `app/src/test/java/app/webora/browser/evidence/ExpressiveBloomJourneyContractTest.kt`.
  - Modified: `docs/tasklist/CI-009.md`, `reports/qa/CI-009.md`.
  - Root cause: the instrumentation expected `By.text("Happy Days Bouquet").clickable(true)`, but the
    deployed Bloom storefront exposes the title link as clickable text `Happy Days`; the full phrase
    appears on the product route and in the image link's accessible label. The product rail is also
    below frame 03's initial viewport, so waiting without user-equivalent scrolling cannot reach it.
  - Fix: use the exact clickable storefront title as the interaction/readiness selector, perform a
    bounded black-box swipe sequence until that node is visibly exposed, and retain the existing
    absent/present readiness checks around product navigation and Back/Forward. Remote text still
    never authorizes capture or trust; browser-owned tags and `ScreenEvidenceGuard` remain decisive.
  - Acceptance: source contract rejects the retired full-name selector and requires the bounded
    scroll helper; focused JVM contract test and Android instrumentation compilation pass; the full
    repository gate remains green. Runtime acceptance remains TASK-3 and requires two fresh cold
    hosted eight-frame runs after this fix.
