# CI-009: Tasklist
Status: TASKLIST_READY

References:
- PRD: `docs/prd/CI-009.prd.md`
- Research: `docs/research/CI-009.md`
- Plan: `docs/plan/CI-009.md`

## Tasks

- [x] TASK-1: expand and pin the guarded M9 visual evidence inventory
  - Modified: `app/src/androidTest/java/app/webora/browser/visual/LiveSiteScreenshotTest.kt`
  - Modified: `app/src/test/java/app/webora/browser/evidence/ExpressiveBloomJourneyContractTest.kt`
  - Modified: `evidence-sheet/src/test/kotlin/app/webora/evidence/MainTest.kt`
  - Modified: `evidence-sheet/src/test/kotlin/app/webora/evidence/ContactSheetTest.kt`
  - Result: the first M9 implementation established an eight-frame diagnostic journey and the
    browser-owned capture guards. TASK-FIX-3 later separates its human-facing showcase from deeper
    navigation smoke coverage without weakening those guards.

- [x] TASK-2: record review, local QA, architecture, and hosted acceptance blocker
  - New: `reports/review/CI-009.md`, `reports/qa/CI-009.md`
  - Modified: `CLAUDE.md`
  - Result: review and locally available gates completed; hosted runtime evidence remains the final
    acceptance authority.

- [ ] TASK-3: complete two-run hosted visual and smoke acceptance and close M9
  - New/modified: `reports/qa/CI-009.md`, `docs/ROADMAP.md`, `docs/tasklist/CI-009.md`
  - Dispatch the final commit twice from cold GitHub-hosted Pixel 6 runners.
  - Each run must execute `LiveSiteHostedSuite`: the six-frame showcase first, then the separate
    navigation smoke test in the same emulator session.
  - Inspect all six full-resolution showcase frames and the six-tile preview; verify the smoke test
    also completes successfully before treating the run as accepted.
  - Canonical showcase inventory:
    1. `01-home.png` — native Webora Home.
    2. `02-siteskin-consent.png` — explicit Bloom SiteSkin consent.
    3. `03-bloom-storefront.png` — integrated Bloom storefront.
    4. `04-bloom-actions.png` — the central branded flower/action control opened with all native
       SiteSkin actions visible.
    5. `05-bloom-profile.png` — Profile action selected and Bloom Account page rendered in integrated
       mode.
    6. `06-google-regular.png` — ordinary Google browsing with regular Webora chrome and no SiteSkin
       layers.
  - Acceptance: two consecutive complete uncontested hosted runs; `test_status=0`; `png_count=6`;
    contact sheet reports `tiles=6`; smoke covers Happy Days product navigation, browser Back/Forward,
    action bouquet, return to native Home, and exact-origin regular-mode teardown; human visual review
    accepts all six frames; roadmap/QA/validation close cleanly.

## Review fixes

- [x] TASK-FIX-1: wait for black-box page transitions before product/history captures
  - Source: `/review finding 1`.
  - Result: the original eight-frame diagnostic journey synchronized product/history transitions on
    live accessibility state instead of trusting stale pixels.

- [x] TASK-FIX-2: align the Happy Days black-box interaction with the deployed storefront
  - Source: hosted `Android screenshots` run #33 (`31929274541`).
  - Root cause: instrumentation expected clickable `Happy Days Bouquet`, while the deployed
    storefront exposes clickable text `Happy Days` below the first viewport.
  - Fix: use the deployed clickable title and bounded user-equivalent scrolling; browser-owned tags
    and `ScreenEvidenceGuard` remain the trust/capture authority.

- [x] TASK-FIX-3: split canonical visual showcase from deep navigation smoke coverage
  - Source: product review of the now-usable browser experience after M9 implementation.
  - New: `app/src/androidTest/java/app/webora/browser/visual/LiveSiteHostedSuite.kt`.
  - New: `app/src/androidTest/java/app/webora/browser/visual/LiveSiteNavigationSmokeTest.kt`.
  - Modified: `app/src/androidTest/java/app/webora/browser/visual/LiveSiteScreenshotTest.kt`.
  - Modified: `app/src/test/java/app/webora/browser/evidence/ExpressiveBloomJourneyContractTest.kt`.
  - Modified: `evidence-sheet/src/test/kotlin/app/webora/evidence/MainTest.kt`,
    `evidence-sheet/src/test/kotlin/app/webora/evidence/ContactSheetTest.kt`.
  - Modified: `scripts/android-screenshot-ci.sh`.
  - Decision: screenshots are a concise six-frame product story rather than a transcript of every
    diagnostic action. The old Happy Days/Back/Forward traversal remains mandatory as a separate
    instrumentation smoke test, so artifact readability improves without reducing regression cover.
  - The final ordinary-site frame uses `https://www.google.com/ncr`; acceptance asserts only
    browser-owned `google.com` identity/navigation and SiteSkin absence, never Google page copy,
    layout, locale, cookies, or other remote content.
  - The action-bouquet frame excludes browser-owned overlay pixels from the rendered-page threshold,
    so opening the native flower menu cannot make a blank WebView pass the evidence guard.
  - Acceptance: source contracts pin exactly six showcase frames, reject the retired eight-frame
    artifact, require the hosted suite to run showcase before smoke, require the smoke to retain
    product/Back/Forward/handoff markers, and forbid the smoke from publishing screenshots.
