# UX-001: Tasklist
Status: TASKLIST_READY

References:
- PRD: `docs/prd/UX-001.prd.md`
- Research: `docs/research/UX-001.md`
- Plan: `docs/plan/UX-001.md`

## Tasks

- [x] TASK-1: Record the selected direction as `ADR-013`
  - New: `docs/adr/ADR-013-browser-owned-design-tokens.md`
  - Modified: `docs/adr/README.md`, `docs/ROADMAP.md`
  - Acceptance: the ADR names direction **B — "Get out of the way"** and the **fixed identity frame**
    amendment taken from direction C, with the rationale for the amendment (it answers B's stated
    cost without adding chrome height, and costs one token outside the light/dark pair). It states
    B's `C1` mechanism as two states — a non-editable display state rendering
    `securityPresentation(state.mode)`, and an edit state where identity moves to its own row — and
    names the violation: dropping the second state leaves the domain inside an editable field and
    breaks `ADR-006`. It records `C2`: `WeboraTheme` compiled, no path from a manifest value,
    `SiteSkinColorScheme` still the entire website-influenceable colour surface. It records why A
    and C were not selected, with the trade each represented. The README row for 013 moves off
    `PROPOSED` and points at the file; `UX-001` is ticked in the roadmap. No source, test, resource
    or build file changes.
  - Tests: `git diff --stat` confirms `docs/` only; `bash scripts/pre-commit-check.sh`
  - Result: `ADR-013` records the selection, the amendment and its rationale, B's two-state `C1`
    mechanism with an explicit **violation condition**, the `C2` compiled-tokens rule with the
    `UX-002` test handed off rather than left implicit, and both rejected directions with the trade
    each represented. The README's 013 row moves to ACCEPTED and links the file; its stub section
    now points at the promoted document rather than duplicating it.
  - Note: the stub predicted its own promotion — *"promote to its own file if the reasoning needs
    re-arguing — the security half of it probably will"*. It does, because the selected direction is
    the one whose `C1` answer is an implementation cost rather than a layout choice.
  - Deviation: none. No source, test, resource or build file was touched.
