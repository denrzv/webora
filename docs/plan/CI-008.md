# CI-008: Implementation plan
Status: PLAN_APPROVED

Input: [`../research/CI-008.md`](../research/CI-008.md) (`RESEARCH_READY`).

## Flow

1. Complete the existing accepted integrated frame and its brand-asset decision.
2. Resolve the browser-owned SiteSkin Back node by its closed test tag.
3. Require that node to be displayed and enabled, then perform the existing user-visible click.
4. Continue unchanged through native Home's address input to the regular example origin.
5. Require browser chrome presence, SiteSkin chrome absence, and guarded rendered frame 04.

## Origin and trust boundary

Bloom's manifest can influence only validated SiteSkin presentation. It cannot influence Back's tag,
enabled state, callback, Home fallback, the regular destination, assertions, or screenshot policy.
The Back prerequisite is browser-observed state and the click remains a real user-visible operation.
The transition crosses from `https://denrzv.github.io` to native Home and then
`https://example.com`; exact-origin activation and teardown remain production responsibilities.

## Security and evidence integrity

- Assert more without making any currently failing frame pass.
- Do not change `ScreenEvidenceGuard`, readiness thresholds, deadlines, dismissal policy, frame
  count, collection, composition, or workflow YAML.
- Do not use page or manifest prose as an acceptance signal.
- Preserve regular-chrome presence and all three SiteSkin-absence assertions before frame 04.

## File-by-file changes

| Path | Change |
|---|---|
| `app/src/androidTest/java/app/webora/browser/visual/LiveSiteScreenshotTest.kt` | Assert the tagged Back node is displayed and enabled before its existing click. |
| `docs/BACKLOG.md` | Add CI-008 as the focused diagnostic hardening follow-up to BROWSE-008. |
| `docs/ROADMAP.md` | Track implementation separately from pending hosted CI-007 rollout evidence. |
| `CLAUDE.md` | Record the evidence rule that transition prerequisites are asserted at their action seam. |
| `docs/tasklist/CI-008.md` | Track implementation and closeout. |
| `reports/review/CI-008.md`, `reports/qa/CI-008.md` | Persist review and QA evidence. |

## Tests

- `./gradlew :app:compileDebugAndroidTestKotlin`
- `./gradlew detekt`
- Static ordering check proving `assertIsEnabled()` precedes `performClick()` on the same node.
- Negative source mutation removing/inverting the enabled prerequisite, followed by restoration.
- `bash scripts/pre-commit-check.sh`

Runtime screenshots remain a hosted rollout check. In managed cloud without a push-capable remote,
connected Android device, or `/dev/kvm`, compile instrumentation and report runtime as an environment
limitation rather than provisioning a software-only emulator.

## Rollout / versioning

No runtime, protocol, schema, persistence, or release-version change. Two consecutive cold hosted
`CI-007` runs remain the downstream M8 acceptance evidence.
