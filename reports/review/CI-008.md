# Review: CI-008
Date: 2026-08-15
Status: RESOLVED

## Summary

The change adds one semantic prerequisite to the existing hosted transition and leaves product
runtime, origin activation, capture policy, workflow configuration, and artifact presentation
untouched. It turns the indirect address-field timeout seen in runs 30 and 31 into a direct failure
at the browser-owned control whose availability is required.

## Architecture

| Concern | Assessment |
|---|---|
| Evidence seam | PASS — the assertion is adjacent to the existing visible action. |
| Product boundary | PASS — no runtime implementation or test-only state seam was added. |
| Scope | PASS — one instrumentation assertion plus workflow artifacts/documentation. |
| Complexity | PASS — no branch, helper, timeout, retry, or production method was introduced. |

## Security

| Property | Assessment |
|---|---|
| Browser ownership | PASS — the fixed tag and enabled semantics belong to browser chrome. |
| Manifest influence | PASS — remote content cannot set the tag, enabled state, callback, or assertion. |
| Origin transition | PASS — exact-origin activation and SiteSkin teardown remain unchanged. |
| Fail closed | PASS — the new check can only refuse earlier; every downstream guard remains. |

## Findings

None.

## Not findings

- Chaining `assertIsDisplayed().assertIsEnabled().performClick()` is intentional: all operations
  resolve the same tagged node at the action seam, avoiding a diagnostic gap between prerequisite
  and invocation.
- This does not duplicate `BROWSE-008` unit coverage. Product tests prove the policy; the hosted
  journey proves that its browser-owned affordance exposes the policy before relying on it.
- Local Android-test compilation is not runtime evidence. The two cold hosted runs remain pending
  and are not reclassified as CI-008 results.
- No additional screenshot is needed because enabled state is semantic behavior, not trustworthy
  pixel decoration, and the journey already captures the accepted integrated state immediately
  beforehand.

## Test coverage

| Check | Coverage |
|---|---|
| Android-test compilation | Compose assertion API and journey source compile. |
| Assertion-order check | The enabled prerequisite precedes the existing click. |
| Removal negative control | Removing `assertIsEnabled()` is rejected by the source contract check. |
| Full pre-commit gate | Units, detekt, secrets, shell, release isolation, and core boundary pass. |

## Verdict

RESOLVED. No open architecture, security, testing, complexity, or evidence-integrity finding.
