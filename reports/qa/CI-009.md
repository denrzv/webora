# QA Report: CI-009
Status: QA_BLOCKED

## Scope

Eight-frame expressive hosted evidence: guarded Home/consent, Bloom storefront, Happy Days product,
Back/Forward restorations, native SiteSkin hub, contact-sheet totality, and exact-origin regular-mode
teardown. Review finding 1 is resolved. Local QA exhausts JVM, instrumentation compilation, lint,
assembly, shell, security, and workflow gates; the ticket's two-run hosted acceptance is not locally
substitutable.

## Test scenarios

| # | Scenario | Method | Result |
|---|---|---|---|
| 1 | Complete chronological frame inventory | `ExpressiveBloomJourneyContractTest` | PASS — exactly eight filenames, in order; partial/reversed negative fixture is refused. |
| 2 | Every WebView frame requires rendered pixels | source contract plus `captureDeviceScreenshot` call review | PASS — frames 03–08 pass `requirePageContent = true`; Home/consent are documented native exceptions. |
| 3 | Storefront/product transition readiness | clickable Happy Days accessibility-node absence/presence waits and source marker test | PASS — capture cannot proceed while the preceding page's link state remains. |
| 4 | Browser-owned integrated prerequisites | instrumentation source/compiled tags and browser resource headings | PASS — identity/header/dock guard every integrated state; native hub requires tag and both browser-authored sections. |
| 5 | Back/Forward action seam | displayed/enabled assertions before each tagged browser control click | PASS. |
| 6 | Exact-origin teardown | regular identity/navigation presence and expressive/header/dock/hub/quick-action absence assertions | PASS in compiled source; runtime pending hosted run. |
| 7 | Contact-sheet order, captions, and totality | `./gradlew :evidence-sheet:test` | PASS — eight shuffled unique markers sort correctly, every caption band has ink, `tiles=8`; existing decode/stale/aspect refusals remain green. |
| 8 | Focused journey negative controls | `./gradlew :app:testDebugUnitTest --tests '*ExpressiveBloomJourneyContractTest'` | PASS. |
| 9 | Android instrumentation source validity | `./gradlew :app:compileDebugAndroidTestKotlin` | PASS. |
| 10 | Static quality and full repository gate | `./gradlew detekt`; `bash scripts/pre-commit-check.sh` | PASS. |
| 11 | Connected local Pixel runtime | `/dev/kvm` probe; `adb devices` | NOT RUN — `/dev/kvm` absent and no connected device; software-only emulator forbidden. |
| 12 | Two consecutive cold hosted eight-frame runs | manual `Android screenshots` workflow on final commit | BLOCKED — checkout has no push-capable remote and `gh auth status` reports no authenticated GitHub host, so the final commit cannot be dispatched or selected. |
| 13 | Human full-resolution/contact-sheet inspection | screenshots artifacts from scenario 12 | BLOCKED — no honest artifact exists until the hosted runs execute. |

## Edge cases
- invalid manifest → N/A — no manifest/discovery/validation logic change. Existing regular fallback
  and pre-commit test suite pass.
- origin change / redirect → final distinct-origin absence/presence assertions compile; runtime
  integrated→regular evidence is blocked with scenario 12. No origin or redirect logic changed.
- offline with cached manifest → N/A — no cache/network logic change. A live outage will fail the
  hosted test and retain diagnostics rather than manufacture evidence.
- oversized or malformed payload → N/A — no parser/limits logic change; core gate passes.
- accessibility (TalkBack, font scale) → the journey uses shipped accessibility semantics and local
  browser resource strings. No production UI changed; runtime TalkBack/font-scale is not the scope
  of the fixed Pixel 6 evidence job.
- partial/undecodable frames → composer throws, removes stale preview, and prints no tile count;
  workflow PNG/tile mismatch remains fatal.
- contested window / blank page → unchanged `ScreenEvidenceGuard` focus, rendered, and candidate
  verdict paths refuse capture and write diagnostics.
- page-controlled hub lookalike → cannot pass: the test requires the native Compose hub tag and
  browser-authored section headings in addition to trusted site labels.

## Result
Status: QA_BLOCKED

Notes: All evidence available in this managed checkout is green. QA cannot become `QA_PASSED`, the
roadmap cannot mark CI-009/M9 complete, and issue #88 should not close on merge until two consecutive
cold runs for the final commit publish and a reviewer accepts all eight full-resolution frames plus
the eight-tile contact sheet. This is the ticket's defining acceptance criterion, not an optional
environment-only runtime check.
