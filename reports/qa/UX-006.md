# QA Report: UX-006
Status: QA_PASSED

## Scope

First-use SiteSkin consent projection, manifest/browser attribution, canonical-origin identity,
bounded text and counts, guarded colour presentation, unchanged consent decisions, and build/static
gates.

## Test scenarios

| # | Scenario | Method | Result |
|---|---|---|---|
| 1 | Adversarial site text cannot escape the display bound or reorder content | `UntrustedTextTest` and `SiteSkinConsentModelTest` | PASS |
| 2 | Preview describes the chrome that would render | `SiteSkinConsentModelTest` title, colour, optional-field, and 5/5/20 count cases | PASS |
| 3 | Complete canonical origin remains the browser-authored title | `SiteSkinConsentDialogTest` source compiled, including non-default-port case | PASS |
| 4 | Allow, Not now, and Never retain their callbacks and persistence flow | Existing consent/state tests plus source review | PASS |
| 5 | Added content remains scrollable and decisions remain reachable at large font scale | Font-scale Compose test source compiled | PASS |
| 6 | Full repository guardrails | `bash scripts/pre-commit-check.sh` | PASS |
| 7 | Runtime instrumentation and Pixel 6 screenshot | `/dev/kvm` and `adb devices` environment probe | NOT RUN — managed environment has no KVM or connected device |

## Edge cases

- invalid manifest → regular browser mode: unchanged; no consent candidate or preview is created.
- origin change / redirect: unchanged; Allow still rechecks the candidate's canonical origin and
  discovery generation before activation.
- offline with cached manifest: unchanged; an already validated candidate projects without I/O and
  the preview performs no asset fetch.
- oversized or malformed payload: unchanged; parser/schema/security validation rejects it before
  this trusted presentation boundary.
- accessibility (TalkBack, font scale): browser and site copy remain separate nodes, the attribution
  is a heading, the decorative swatch clears semantics, colour is also described in text, and the
  content scrolls above wrapping 48 dp actions. Instrumentation sources compile; runtime TalkBack,
  large-font interaction, and screenshot inspection are unavailable without a device.

## Result

Status: QA_PASSED

Notes: all locally runnable checks pass. Runtime visual evidence is unavailable in this managed
environment, and repository policy prohibits provisioning a software-only emulator without KVM.
