# QA Report: NET-002
Status: QA_PASSED

## Scope

In-memory accepted-manifest caching, exact origin/version identity, bounded HTTP freshness, conditional revalidation, explicit offline reuse, and cancellable discovery integration.

## Test scenarios

| # | Scenario | Method | Result |
|---|---|---|---|
| 1 | Distinct origin and version identities | `ManifestCacheTest` negative controls | PASS |
| 2 | One active version per origin | replacement test with second-origin control | PASS |
| 3 | Browser owns cached bytes | caller/source mutation test | PASS |
| 4 | TTL and 24-hour ceiling | injected-clock boundary assertions | PASS |
| 5 | Malformed/negative/duplicate/overflow max-age | directive table | PASS — stale |
| 6 | ETag and Last-Modified validators | MockWebServer recorded request | PASS |
| 7 | HTTP versus transport failure | closed-result transport test | PASS |
| 8 | Fresh exact-origin hit | coordinator transport assertions | PASS |
| 9 | Offline versus rejected stale reuse | coordinator negative control | PASS |
| 10 | 304 metadata refresh | fetched → stale → 304 → fresh flow | PASS |
| 11 | Cancellation and stale jobs | existing coordinator/call tests | PASS |
| 12 | Full repository guardrails | `bash scripts/pre-commit-check.sh` | PASS |

## Edge cases

- invalid manifest → rejected, not cached, and regular-browser outcome emitted.
- origin change / redirect → lookup uses the new full canonical origin; transport retains exact-origin redirects.
- offline with cached manifest → only explicit transport unavailability reuses that exact origin's stale entry.
- oversized or malformed payload → size bounds and validation fail closed; malformed directives are stale.
- accessibility (TalkBack, font scale) → N/A — no user-facing UI or semantics changed.
- process restart → cache intentionally clears; durable browsing-history policy is out of scope.
- runtime instrumentation/screenshots → N/A — no visible UI changed and seams are JVM-testable.

## Result

Status: QA_PASSED

Notes: Focused tests and the full local gate pass. This managed checkout has no configured remote, so branch CI cannot be inspected; local task commits are valid checkpoints.
