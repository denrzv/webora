# QA Report: UX-016
Status: QA_PASSED

## Scope

Validated equal regular-dock distribution on the shared Home/ordinary-browser path, fixed command
behavior and ownership, accessibility targets, compact geometry, regression gates, and the visual
evidence boundary.

## Test scenarios

| # | Scenario | Method | Result |
|---|---|---|---|
| 1 | Dock consumes full inner pill width | Compose bounds: first slot left equals dock left; last slot right equals dock right | PASS (compiled instrumentation). |
| 2 | Six slots are equal and centered | Compose bounds compare each width and adjacent center delta within one physical pixel | PASS (compiled instrumentation). |
| 3 | Outer geometry is symmetric | Compose bounds compare first/last center inset | PASS (compiled instrumentation). |
| 4 | Fixed order and callbacks | Existing interaction test plus JVM source negative control | PASS. |
| 5 | Disabled history/reload behavior | Existing enabled-state Compose assertions | PASS (compiled instrumentation). |
| 6 | Minimum targets | Existing width/height assertions require at least 48 dp for all six controls | PASS (compiled instrumentation). |
| 7 | Compact and large-text operability | 320 dp geometry/containment tests and existing 200% font-scale dock test compile | PASS (compiled instrumentation); runtime unavailable. |
| 8 | Home and regular share result | JVM source contract pins both to `BrowserNavigationShell` inside one safe-drawing boundary | PASS. |
| 9 | Full regression/security/lint gate | `bash scripts/pre-commit-check.sh` | PASS. |
| 10 | Home visual screenshot | `adb devices -l`; `/dev/kvm` availability check | ENVIRONMENT LIMITATION — no connected device and no `/dev/kvm`; per repository policy no software-only emulator was provisioned. |

## Edge cases

- invalid manifest → regular browser mode: N/A — no manifest parsing or mode transition change; the
  regular dock remains independent of manifest state.
- origin change / redirect: N/A — no navigation/origin logic change; ordinary mode continues through
  the same shared shell and protected integrated mode remains separate.
- offline with cached manifest: N/A — no network/cache change.
- oversized or malformed payload: N/A — no manifest/payload change.
- accessibility (TalkBack, font scale): PASS in compiled coverage — localized content descriptions,
  enabled semantics, existing 48 dp buttons, compact containment, and the existing 200% font-scale
  scenario remain compiled; runtime TalkBack/visual inspection is unavailable without a device.
- menu expanded: PASS in existing interaction coverage — More still opens the browser overflow and
  Settings invokes the same callback; the popup is not a seventh command slot.
- pixel rounding: PASS — one-pixel tolerance accepts only weight rounding while full edges and equal
  center deltas prevent the original leading-packed layout from passing.

## Commands and results

- `./gradlew :app:testDebugUnitTest :app:compileDebugAndroidTestKotlin` — PASS.
- `bash scripts/pre-commit-check.sh` — PASS after implementation; includes secret scan, shell checks,
  readiness self-tests, core/unit tests, release inspector isolation, and detekt.
- `adb devices -l` — no connected devices.
- `test -e /dev/kvm` — unavailable; runtime instrumentation and Home screenshot not attempted by policy.

## Result

Status: QA_PASSED

All functional and compiled acceptance evidence is green. The requested visual QA screenshot cannot
be produced in this managed-cloud checkout because it has neither a connected Android device nor
hardware virtualization. This is an explicitly permitted environment limitation, not substituted
with manufactured runtime evidence.
