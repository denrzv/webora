# QA Report: UX-008
Status: QA_PASSED

## Scope

Integrated-mode browser Back presentation, state, dispatch, manifest independence, security identity
regression, and build/static gates.

## Test scenarios

| # | Scenario | Method | Result |
|---|---|---|---|
| 1 | Back is present and dispatches when history exists | Compose test source plus `compileDebugAndroidTestKotlin` | PASS |
| 2 | Back stays present but disabled at history root | Compose test source plus `compileDebugAndroidTestKotlin` | PASS |
| 3 | Manifest styling cannot control Back | JVM contract with hostile negative snippet | PASS |
| 4 | Browser-authored domain/TLS identity remains displayed | Existing and updated SiteSkin top-bar tests compile; app unit suite | PASS |
| 5 | Full repository guardrails | `bash scripts/pre-commit-check.sh` | PASS |
| 6 | Pixel 6 visual/runtime instrumentation | `/dev/kvm` and `adb devices` environment probe | NOT RUN — managed environment has no KVM or connected device |

## Edge cases

- invalid manifest → regular browser mode: unchanged; the integrated branch is never selected.
- origin change / redirect: unchanged; activation still follows committed-origin browser mode and
  Back delegates to WebView history rather than a manifest action.
- offline with cached manifest: unchanged; no discovery, cache, or transport logic changed.
- oversized or malformed payload: unchanged; validation rejects before this presentation exists.
- accessibility (TalkBack, font scale): localised `Back` description and `WeboraIconButton`'s 48 dp
  target are reused; instrumentation sources compile. Runtime TalkBack/font-scale and screenshot
  inspection are unavailable without a device.

## Result

Status: QA_PASSED

Notes: all locally runnable checks pass. The missing managed Android runtime limits visual evidence,
not code validation; repository instructions prohibit provisioning a software-only emulator here.
