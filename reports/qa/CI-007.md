# QA Report: CI-007
Status: QA_PASSED

## Scope

The fourth canonical regular-browsing frame, its integrated-to-regular transition assertions,
four-frame contact-sheet ordering/count, unchanged screenshot acceptance boundary, documentation,
and the absence of runtime product/protocol changes.

## Test scenarios

| # | Scenario | Method | Result |
|---|---|---|---|
| 1 | Existing Home, consent, and integrated frames remain in order | `LiveSiteScreenshotTest` source review and Android-test compilation | PASS |
| 2 | Journey proves Bloom integrated chrome before leaving | displayed integrated security/bottom tags before frame 03 and transition | PASS |
| 3 | Browser-owned UI reaches ordinary HTTPS destination | SiteSkin Back plus visible address input/IME Go; no controller injection | PASS (compiled) |
| 4 | Regular security identity and browser navigation are present | `BROWSER_SECURITY_TAG`, localised `Secure · example.com`, and `BROWSER_NAVIGATION_SHELL_TAG` assertions | PASS (compiled) |
| 5 | Prior SiteSkin chrome was removed | absence assertions for integrated security, bottom navigation, and quick actions | PASS (compiled) |
| 6 | Ordinary page is rendered and Webora owns accepted bitmap | frame 04 calls unchanged `captureDeviceScreenshot(..., requirePageContent = true)` | PASS by unchanged tested policy; runtime pending |
| 7 | Contact sheet contains four frames once and in filename order | `ContactSheetTest.composesOneTilePerFrameInFilenameOrder` | PASS |
| 8 | Wrong fourth-frame identity is detected | temporary mutation expected frame 03's marker at tile 4 | PASS — test failed at assertion and returned green after restore |
| 9 | Command reports the current canonical count | `MainTest.printsOneTilesLineOnSuccess` | PASS — exact `tiles=4` |
| 10 | App units, Android-test sources, lint, security, and release checks | focused Gradle checks and `bash scripts/pre-commit-check.sh` | PASS |
| 11 | Hosted cold Pixel 6 journey publishes four uncontested frames | manual `android-screenshots` workflow | NOT RUN — checkout has no push-capable remote; no hosted run can target these local commits |
| 12 | Local runtime screenshot/visual inspection | `adb devices`, `/dev/kvm` | NOT RUN — no connected device and `/dev/kvm` absent; software-only emulator forbidden by repository policy |

## Acceptance mapping

| PRD criterion | Evidence |
|---|---|
| Existing three frames plus regular frame | Four ordered test constants compile; composer/command fixtures prove a four-frame inventory. Hosted production is explicitly pending. |
| Regular identity and shell visible | Stable displayed-tag and localised identity assertions precede capture. |
| SiteSkin layers absent after proven integration | Three absence assertions follow frame 03's positive integrated assertions. |
| Unchanged ownership/rendered gate and separate diagnostics | Frame 04 reuses `captureDeviceScreenshot`; `ScreenEvidenceGuard`, workflow policy, collector, and upload paths are unchanged. |
| Contact sheet and summary account for four | Composer fixture returns four, command emits `tiles=4`, workflow dynamically compares tile/PNG counts and summary reads actual PNG count. |
| Pre-commit gate | PASS. |

## Edge cases

- invalid manifest → regular browser mode: PASS — frame acceptance needs only browser-observed regular
  semantics; invalid/missing example-origin discovery cannot construct SiteSkin configuration.
- origin change / redirect: PASS for the intended exact-origin transition — `example.com` differs
  from Bloom and all SiteSkin tags must disappear. A redirect away from the expected origin would
  fail the fixed `example.com` security assertion rather than produce mislabeled evidence.
- offline with cached manifest: PASS fail-closed — Bloom/example network failure prevents the required
  semantics/rendered frame and yields diagnostics; the harness does not manufacture a substitute.
- oversized or malformed payload: N/A — no parser or validation logic change. Existing rejection
  remains regular mode and cannot bypass the fixed identity/layer assertions.
- accessibility (TalkBack, font scale): PASS by retained stable tags, browser resource strings, and
  existing accessible address/Back/navigation semantics. Runtime TalkBack and visual inspection are
  environment-limited.
- keyboard remains visible: PASS fail-closed by design — the journey waits for destination chrome and
  the existing window-ownership guard must accept the final bitmap; it adds no keyboard dismissal.
- example page decoration changes: PASS — no title, body, colour, geometry, or page string is asserted;
  only rendered-content threshold and browser-owned origin chrome matter.
- SiteSkin discovery races: PASS — regular presence and all three SiteSkin absences are asserted in
  the same settled journey stage before capture; a late activation would violate those assertions or
  the captured browser layer contract.
- contact-sheet stale preview: PASS — existing composer deletes stale output before composition and
  excludes `preview.png`; its unchanged regression suite remains green.
- diagnostic leakage into review artifact: PASS — collector and two disjoint upload roots are
  unchanged; frame 04 follows the existing screenshot storage path.

## Result
Status: QA_PASSED

Notes: All checks runnable in this managed-cloud checkout are green. This ticket changes a hosted
instrumentation journey, so final rollout evidence still requires the manually dispatched workflow on
a durable branch. This report does not claim that run occurred. With no remote, device, or `/dev/kvm`,
repository policy requires compilation plus an explicit environment limitation rather than a
software-only emulator or staged screenshot.
