# QA Report: UX-015
Status: QA_PASSED

## Scope

Validated the protected-integrated dock/hub placement, browser/site ownership boundary, history and
typed callback seams, route semantics, compact accessibility contracts, regular-mode teardown, and
the full repository gate for commits `5888dac..d2a3d2b`.

## Acceptance criteria evidence

| Criterion | Evidence | Result |
|---|---|---|
| 1. Fixed Back/Forward/hub/Tabs/More dock and 48 dp targets | `SiteSkinDock.kt`; fixed-order JVM negative control; compiled `SiteSkinDockTest` 320 dp/200% target/callback case | PASS |
| 2. Observed history and existing Tabs/More surfaces | `RegularBrowser` passes observed history, current controller and existing `onTabs`; session/handoff unit suites | PASS |
| 3. Trusted bounded items, active route, typed dispatch | Existing chrome-model tests; compiled `SiteSkinNavigationHubTest`; resolver/NavMatcher suites | PASS |
| 4. Separate browser section and fixed dock independence | Hub headings/divider and typed callbacks; negative control rejects generic manifest dock lists | PASS |
| 5. Tab/origin invalidation and no regular leakage | Visibility-only state; close paths; compiled regular teardown; session/handoff suites | PASS |
| 6. Compact/large text/theme/reduced motion | Compiled 320 dp/200% dock test; UX-013 presentation matrix | PASS with runtime limitation |
| 7. Positive/negative ownership, selection and dispatch controls | Source negative fixture, compiled dock/hub tests, full 330-test unit run, release check | PASS |
| 8. Mandatory gate | `bash scripts/pre-commit-check.sh` after `TASK-FIX-1` | PASS |

## Test scenarios

| # | Scenario | Method | Result |
|---|---|---|---|
| 1 | Five fixed browser commands stay ordered and site-independent | JVM source contract plus compiled dock test | PASS |
| 2 | History state and named callbacks are isolated | Compiled `SiteSkinDockTest` | PASS |
| 3 | Route selection does not leak onto actions | Compiled `SiteSkinNavigationHubTest` | PASS |
| 4 | Site selection returns the original typed item | Compiled `SiteSkinNavigationHubTest` id assertion | PASS |
| 5 | Integrated projection replaces old site layers | Source contract and compiled layout test | PASS |
| 6 | Regular projection restores browser shell | Compiled layout test; handoff unit test | PASS |
| 7 | Tab observations remain addressed | Existing `BrowserSessionTest` in full unit gate | PASS |
| 8 | Release variants exclude unsupported inspector | Pre-commit release inspection | PASS |
| 9 | Full security/style/test gate | Gitleaks, shellcheck, readiness, core/app tests, release inspection, detekt | PASS |

## Edge cases

- invalid manifest → regular browser mode: PASS — no validator change; existing suites pass.
- origin change / redirect: PASS — exact-origin handoff is unchanged; hub closes on page start and
  requires current integrated mode/configuration.
- offline with cached manifest: N/A — no manifest transport/cache logic change.
- oversized or malformed payload: PASS — no parser change; core guard suites pass.
- empty navigation/actions: PASS — five browser commands/browser section are unconditional.
- unknown action/icon: PASS — existing normalized icon fallback and typed resolver remain authoritative.
- stale tab/controller callback: PASS — the hub stores visibility only and dispatches through current
  composition state.
- accessibility (TalkBack, font scale): PASS for compiled semantics/48 dp/200% coverage. Runtime
  TalkBack and pixel inspection are unavailable because there is no device or `/dev/kvm`.
- light/dark/reduced motion: PASS through existing presentation tests and compiled reduced-motion dock
  fixture; runtime visual comparison is unavailable for the same device limitation.

## Result

Status: QA_PASSED

Notes: All runnable gates pass, review is `RESOLVED`, and every criterion maps to source, JVM, or
compiled instrumentation evidence. Runtime instrumentation and a screenshot could not be captured
because `adb devices` reports no connected device and `/dev/kvm` is absent. Repository policy treats
this as a managed-cloud limitation and forbids provisioning a software-only emulator.
