# QA Report: UX-003
Status: QA_PASSED

## Scope

Browser-owned regular-mode address/identity chrome, bottom navigation dock, overflow dispatch,
persistent status region, error/recovery presentation, security separation, 48 dp wrapper use and
200% font-scale instrumentation compilation. SiteSkin integrated chrome is regression scope only.

## Test scenarios

| # | Scenario | Method | Result |
|---|---|---|---|
| 1 | Committed identity differs from edited address | `BrowserChromeTest.identitySurvivesAddressEditingAndUsesCommittedOrigin` compiled; JVM source guard executed with broken binding control | PASS — tagged chip remains committed-origin derived. |
| 2 | History enabled states and reload/home/menu dispatch | `BrowserChromeTest.dockPreservesCommandsAndHistoryEnabledStates` instrumentation compilation plus JVM conventions | PASS — callbacks/states compile and closed menu remains shared. |
| 3 | Top chrome, page evidence and bottom dock ordering | `BrowserChromeContractTest` with nested-dock and top-dock negative controls | PASS — `top < BROWSER_CONTENT_TAG < dock`. |
| 4 | TLS error recovery | `BrowserChromeTest.errorPagePreservesRecoveryActionsAndBoundedIdentity`; existing failure-state JVM suite | PASS — browser copy/domain and retry/home tags preserved. |
| 5 | Live-region persistence and polarity | Existing `BrowserAccessibilityTest` through full unit suite | PASS — polite loading/completion and assertive failure unchanged. |
| 6 | 200% font scale | `BrowserFontScaleTest.regularChromeStaysReachableAtDoubleFontScale` compiled into Android tests | PASS at compile; runtime unavailable in this managed checkout. |
| 7 | Full build and D8 packaging | `./gradlew :app:assembleDebug :app:compileDebugAndroidTestKotlin` | PASS. |
| 8 | Repository guardrails | `bash scripts/pre-commit-check.sh` after every implementation/fix task | PASS. |

## Edge cases

- invalid manifest → regular browser mode: PASS — activation/trust pipeline is unchanged; regular
  chrome consumes no manifest value and existing unit suite is green.
- origin change / redirect: PASS — `BrowserState.observePage` and `securityPresentation` are
  unchanged; identity is recomputed only from committed `BrowserMode`.
- offline with cached manifest: N/A — no cache, discovery or offline logic changed; browser-owned
  error presentation preserves existing state/retry callbacks.
- oversized or malformed payload: N/A — no parser or validation logic changed; core tests passed
  with `ANDROID_HOME` unset through the pre-commit gate.
- accessibility (TalkBack, font scale): PASS for semantics/unit/source checks and Android-test
  compilation. Runtime TalkBack/font-scale instrumentation is unavailable because this managed
  checkout has neither `/dev/kvm` nor a connected Android device; per repository policy no
  software-only emulator was provisioned.
- address field focused/edited: PASS — distinct typed identity node stays composed and the negative
  control rejects tagging the editable field.
- unavailable history action: PASS — disabled semantics are used only for genuinely unavailable
  back/forward commands; enabled dock actions use opaque browser-owned colour.
- screenshot evidence: NOT RUN — no `/dev/kvm` or connected device. The structural JVM guard proves
  dock placement outside the measured page region, but no runtime screenshot is claimed.

## Result
Status: QA_PASSED
Notes: All locally executable gates and Android compilation/package checks pass. Runtime device
instrumentation and screenshots are explicitly unavailable environment evidence, not silently
skipped test claims.
