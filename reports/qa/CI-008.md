# QA Report: CI-008
Status: QA_PASSED

## Scope

The explicit displayed/enabled Back prerequisite in the hosted integrated-to-regular journey,
including preservation of existing browser controls and evidence gates.

## Test scenarios

| # | Scenario | Method | Result |
|---|---|---|---|
| 1 | Integrated Back node exists and is visible | Chained Compose semantic assertion | PASS (compiled) |
| 2 | First-page integrated Back exposes the BROWSE-008 fallback | `assertIsEnabled()` immediately before click | PASS (compiled) |
| 3 | The same visible user action performs the transition | Existing `performClick()` remains in the chain | PASS (compiled) |
| 4 | Missing enabled prerequisite is detected | Temporary source mutation removes `assertIsEnabled()` | PASS — contract check failed, source restored |
| 5 | Existing regular presence and SiteSkin absence remain | Source review after the click | PASS |
| 6 | Capture ownership/rendered and readiness policies remain | Diff review of policy/workflow files | PASS — untouched |
| 7 | Android-test source and static quality compile | focused Gradle commands | PASS |
| 8 | Repository guardrails | `bash scripts/pre-commit-check.sh` | PASS |
| 9 | Live Pixel 6 transition and four canonical frames | hosted `android-screenshots` workflow | NOT RUN — no push-capable remote in this checkout |
| 10 | Local device runtime and screenshot inspection | connected device or `/dev/kvm` | NOT RUN — neither is available; software-only emulator forbidden |

## Acceptance mapping

| PRD criterion | Evidence |
|---|---|
| Displayed and enabled before click | One tagged node chain asserts both semantics before `performClick()`. |
| Visible controls; no injected state | Existing click, Home wait, address input, and IME action are unchanged. |
| Evidence policies unchanged | Guard, readiness, workflow, deadlines, artifact, and composer files have no diff. |
| Compilation and negative control | Android-test compile passes; removal mutation is detected and restored. |
| Honest rollout distinction | Backlog, roadmap, architecture context, review, and QA retain hosted runs as pending. |
| Pre-commit gate | PASS. |

## Edge cases

- invalid manifest: N/A — no parsing or activation logic change; the journey cannot reach the tagged
  integrated prerequisite without accepted SiteSkin state.
- origin change or redirect: PASS fail-closed by unchanged regular identity and SiteSkin-absence
  assertions after the action.
- offline with cached manifest: PASS fail-closed by existing semantic waits and rendered guard; no
  retry, injection, or substitute frame was added.
- oversized or malformed payload: N/A — no manifest pipeline change.
- accessibility: PASS for this change — enabled/displayed semantics are the accessibility-facing
  action state; runtime TalkBack remains environment-limited.
- keyboard remains visible: PASS fail-closed through the unchanged screen-ownership guard.
- Back is displayed but disabled: PASS — the new assertion fails at the cause before click.
- Back is enabled but callback regresses: PASS fail-closed — the unchanged Home address wait fails,
  separating action availability from transition completion.
- WebView history exists: N/A to harness logic; the product policy remains covered by BROWSE-008 and
  the journey intentionally exercises first-page Home fallback.
- stale SiteSkin chrome after transition: PASS fail-closed through the unchanged three absence checks.

## Result

Status: QA_PASSED

All checks runnable in this managed-cloud checkout pass. Runtime instrumentation and visual review
remain explicit environment limitations because there is no connected device, `/dev/kvm`, or
push-capable remote. This report does not claim the pending hosted CI-007 runs occurred.
