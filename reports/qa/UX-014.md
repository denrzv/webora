# QA Report: UX-014
Status: QA_PASSED

## Scope

QA covers the curved branded integrated top replacement, browser-owned canonical identity and Back,
closed presentation/theme/motion wiring, compact large-text behavior, active-tab/exact-origin/stale-
asset preservation, and the unchanged reachability of existing tabs/overflow surfaces. Runtime
instrumentation and screenshot capture are unavailable because this managed-cloud checkout has no
connected Android device and no `/dev/kvm`; per repository policy, no software-only emulator was
provisioned. The instrumentation sources and application were compiled successfully.

## Test scenarios

| # | Scenario | Method | Result |
|---|---|---|---|
| 1 | Integrated top uses UX-013 curved primitive and closed projection; regular/Home handoff unchanged (AC-1) | `SiteSkinNavigationContractTest`, `ChromeHandoffTest`, source inspection, full unit/pre-commit gates | PASS |
| 2 | Canonical domain/TLS is unconditional, separately tagged, browser-token styled, and hostile branding cannot replace it (AC-2) | `SiteSkinTopBarTest` semantics cases compiled; source negative control executed by JVM | PASS |
| 3 | Back is first, visible at root, browser-owned, 48 dp, and dispatches only when enabled; existing tabs/overflow remain separately reachable (AC-3) | `SiteSkinTopBarTest` compiled, `SiteSkinNavigationContractTest` executed, unchanged menu/shell source and existing unit suite | PASS |
| 4 | Loading/error, exact-origin departure, tab switching, and stale asset completion preserve mode isolation (AC-4) | Full unit suite including handoff/state/asset publication tests; unchanged status/error composition inspection | PASS |
| 5 | 320 dp/200% light and dark fixtures keep Back, brand, and identity present and ordered; colours use guarded projection (AC-5) | Three-pair `SiteSkinTopBarTest` instrumentation source compiled; `ExpressiveSiteSkinPresentationTest` and primitive contracts executed | PASS (runtime unavailable) |
| 6 | Reduced mode selects closed immediate policy without removing/delaying slots (AC-6) | `MotionPreferenceTest`, presentation tests, compact dark/reduced compiled fixture | PASS |
| 7 | Unsafe SiteSkin-coloured/conditional identity and permissive missing animator state fail negative controls (AC-7) | Focused JVM source/pure-policy tests | PASS |
| 8 | Available unit, Android-test compilation, lint, secret, shell, core-boundary, assembly, and release-inspector gates (AC-8) | `./gradlew :app:compileDebugAndroidTestKotlin`; `bash scripts/pre-commit-check.sh` | PASS; no runtime device/screenshots available |
| 9 | Mandatory repository gate (AC-9) | `bash scripts/pre-commit-check.sh` after TASK-FIX-1 | PASS |

## Edge cases

- invalid manifest → unchanged; validator rejection still produces regular browser mode before this
  UI seam. Covered by the existing full unit/pre-commit suite.
- origin change / redirect → exact-origin observation still updates `BrowserMode` before
  `ChromeHandoff`; the integrated header is authorized only by `PROTECTED_INTEGRATED`. Existing
  state/handoff tests pass.
- offline with cached manifest → N/A — UX-014 changes neither manifest caching nor network behavior;
  current fallback/activation policy is unchanged.
- oversized or malformed payload → N/A — parsing/size limits are unchanged and occur before the
  trusted configuration accepted by this component. Core tests pass with Android SDK unset.
- accessibility (TalkBack, font scale) → browser security content description and decorative logo
  semantics remain pinned; Back retains its localized accessible label and 48 dp target; 200% font
  fixtures for light/standard, dark/standard, and dark/reduced compile. Runtime TalkBack/manual
  visual verification is unavailable without a connected device.
- extreme logo aspect ratio → existing bounded 40 dp slot assertion remains in compiled
  instrumentation; accepted assets cannot resize the header slot.
- history root → Back is still composed and disabled, not removed; existing compiled test covers it.
- missing/invalid animator state → conservative reduced policy; JVM positive/negative cases pass.
- stale asynchronous brand asset → existing configuration identity check remains unchanged and its
  JVM publication tests pass.
- loading and error → `BrowserStatusRegion` and `BrowserErrorPage` remain outside/after top chrome;
  the implementation changes no error callbacks, retry target, or content overlay behavior.

## Result

Status: QA_PASSED

Notes: All available automated gates are green and every PRD criterion has executable or compiled
evidence. Runtime instrumentation and the requested screenshot for this perceptible UI change could
not be captured solely because the managed-cloud environment has neither a connected device nor
`/dev/kvm`; this is the repository-defined environment limitation, not a product test failure.
