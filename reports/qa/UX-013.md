# QA Report: UX-013
Status: QA_PASSED

## Scope

Validated the closed expressive SiteSkin presentation projection, browser-owned curved-header and
floating-dock geometry, compact/large-text Compose coverage, review fix, and the intentional absence
of production integration. UX-013 changes no protocol, discovery, validation, consent, origin,
network, asset, action-dispatch, persistence, or currently rendered browser surface.

## Test scenarios

| # | Scenario | Method | Result |
|---|---|---|---|
| 1 | Trusted brand colours project deterministically in light and dark modes | `ExpressiveSiteSkinPresentationTest` focused JVM run and full unit gate | PASS — existing guarded schemes are selected exactly. |
| 2 | Missing branding remains total | JVM test validates two repeated projections and existing defaults | PASS. |
| 3 | Reduced motion remains browser-selected and closed | JVM test checks the exact two-value policy and both factory branches | PASS. |
| 4 | Remote/layout/native data cannot enter presentation | Reflective production scan plus unsafe fixture negative control | PASS — unsafe fixture reports `remoteResource` and `rawCss`; production reports none. |
| 5 | Curved geometry stays non-flat and usable at 320/720 bounds | JVM pure-coordinate assertions, real `Outline.Generic` type, and zero-depth negative control | PASS — shoulders preserve 96 px at the compact test height and control remains centered/deeper. |
| 6 | Primitive cannot read configuration, global insets, browser theme, or arbitrary shape/motion inputs | Source contract plus unsafe-source negative control | PASS. |
| 7 | 320 dp / 200% font / 48 dp targets / light-dark-reduced layout | `./gradlew :app:compileDebugAndroidTestKotlin` | PASS (compiled). Runtime not executed: `adb devices` reported no connected device, and managed-cloud policy forbids provisioning a software-only emulator. |
| 8 | Floating dock owns its separation | Review-fix instrumented assertions compare tagged dock bounds to a caller-unpadded 320 dp parent | PASS (compiled); both edge predicates and five target assertions compile against production. |
| 9 | Existing product behavior remains unchanged | Call-site search and full pre-commit build/test/detekt gate | PASS — expressive composables have no production caller. |
| 10 | Complete repository guardrail | `bash scripts/pre-commit-check.sh` after every code task/fix and final docs | PASS. |

## Acceptance criteria mapping

1. Closed trusted projection: scenarios 1–4.
2. Existing normalized roles only; browser policy compiled: scenarios 1, 4, and 6.
3. Deterministic header/dock primitives: scenarios 5 and 8.
4. Compact width, large text, themes, reduced motion, insets, and targets: scenarios 3, 7, and 8;
   device runtime is the documented managed-cloud limitation, not silently claimed evidence.
5. Decorative/instant motion contract and no delayed owned content: scenario 3 plus structural
   review; no transition is introduced by the intentionally unwired primitives.
6. Negative controls for geometry, tokens, order/identity, and motion: scenarios 4–6.
7. Existing chrome unchanged: scenario 9.
8. Command gate: scenario 10.

## Edge cases

- invalid manifest → regular browser mode: Unchanged. UX-013 accepts only a trusted configuration
  downstream of the existing rejection/fallback pipeline; the full existing unit suite passes.
- origin change / redirect: Unchanged. No activation/integration caller was added; existing exact-
  origin teardown remains authoritative and passes in the full suite.
- offline with cached manifest: N/A — no network, discovery, cache, or persistence logic change.
- oversized or malformed payload: Unchanged. Core's size/parse/validation gates run in the mandatory
  pre-commit check and no new input bypass exists.
- accessibility (TalkBack, font scale): Browser/site ownership remains distinguishable by model and
  slot boundary. The 200% font, required-content, fixed-order, and 48 dp target instrumentation
  compiles; runtime TalkBack/device execution is unavailable because no device is connected.
- compact width: 320 dp is explicit in instrumented layout and JVM geometry scenarios.
- dark theme: Both pure projection and instrumented composition branches compile and are asserted.
- reduced motion: The closed browser-owned policy selects `REDUCED`; no current transition exists,
  and downstream integration is required to use an immediate state change.
- system insets: The source negative control rejects a primitive `safeDrawing` read; the existing
  single screen-level inset application remains unchanged.

## Result

Status: QA_PASSED

Notes: All runnable local gates pass. Android instrumentation was compiled but not executed because
this managed-cloud checkout has no connected device; no software-only emulator was provisioned, per
repository policy. This ticket makes no perceptible runnable-app change because integration is
explicitly deferred to UX-014/UX-015, so a screenshot of current chrome would not evidence it.
